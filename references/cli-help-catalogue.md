# Complete CLI Help Catalogue

This is the complete, released option catalogue for every supported command
and subcommand. It is generated from each executable's `--help`, so it
lists positional inputs, flags, defaults, allowed values, and the exact
wording an operator sees. The parser remains the executable contract.

Do not edit this file by hand. After changing a parser or shared option
help, run:

```bash
python scripts/agent_surface_check.py --write-cli-help-catalogue
```

`scripts/release_check.py` compares this file with live help and fails on
any drift. Command purpose, phase placement, and safety context remain in
[Command-Line Reference](command-line-reference.md).

## `scan_profile.py`

```text
usage: scan_profile.py [-h] [--out OUT] [--sample SAMPLE] [--shallow] [--quiet] target

Phase 0.5 scan corpus profiler.

positional arguments:
  target           PDF file or directory of PDFs

options:
  -h, --help       show this help message and exit
  --out OUT        output JSON path
  --sample SAMPLE  profile only the first N pages per file (calibration runs)
  --shallow        skip pixel decoding; much faster, but cannot detect false
                   colour
  --quiet          Suppress the console summary only. Retained artifacts,
                   findings, and exit status are unchanged.
```

## `ingest_pages.py`

```text
usage: ingest_pages.py [-h] --out OUT input

Split a multipage PDF into per-page PDFs and a conservative classification
queue.

positional arguments:
  input       source PDF; it is never modified

options:
  -h, --help  show this help message and exit
  --out OUT   new or empty directory for page PDFs and JSON manifests
```

## `reassemble_pages.py`

```text
usage: reassemble_pages.py [-h] --out OUT --exceptions EXCEPTIONS [--broad-unordered-proposals]
       [--quiet] manifest

Propose evidence-backed page groups and duplicate review candidates.

positional arguments:
  manifest              ingestion_manifest.json from scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new reassembly proposal JSON
  --exceptions EXCEPTIONS
                        new unresolved reassembly/duplicate JSON
  --broad-unordered-proposals
                        also propose review-required unordered groups with
                        exactly one shared typed identifier
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `preprocess_pages.py`

```text
usage: preprocess_pages.py [-h] --out OUT [--dpi DPI] --manifest MANIFEST pages

Create OCR-preprocessing variants without changing source pages.

positional arguments:
  pages                directory of immutable one-page PDFs from intake

options:
  -h, --help           show this help message and exit
  --out OUT            new or empty variant directory
  --dpi DPI            Resolution used to create each image variant.
  --manifest MANIFEST  Destination path for the variant manifest.
```

## `detection_calibration.py`

```text
usage: detection_calibration.py [-h] --out OUT [--thresholds THRESHOLDS] [--minimum-precision
       MINIMUM_PRECISION] [--minimum-recall MINIMUM_RECALL] truth predictions

Calibrate a detection proposal against labeled golden-set items.

positional arguments:
  truth                 Labeled ground-truth artifact the proposals are scored
                        against.
  predictions           Detector proposal artifact scored against the labeled
                        truth.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --thresholds THRESHOLDS
                        Threshold grid to evaluate.
  --minimum-precision MINIMUM_PRECISION
                        Precision a threshold must reach to be reported as
                        acceptable.
  --minimum-recall MINIMUM_RECALL
                        Recall a threshold must reach to be reported as
                        acceptable.
```

## `extraction_context.py`

```text
usage: extraction_context.py [-h] [--registry REGISTRY] [--classifications CLASSIFICATIONS] [--notes
       NOTES] --out OUT --provenance PROVENANCE [--quiet]

Build the corpus context that conditions a context-aware extraction update.
Extraction reads one page at a time and remembers nothing between pages. A
label printed on six hundred pages is therefore rediscovered, or missed, six
hundred times. On a real 716-page commission corpus that cost most of the run:
the acknowledgement number -- the identifier the business ties money to -- was
printed under ``ACK#``, ``ACK NO`` and dealer-qualified variants throughout,
and reached the controlled header on four documents out of 716. Both engines
saw it. Neither could place it, so both filed it in the extension channel, and
every downstream control that needs an attribution key found none. This
command turns what the run has already *learned* about its own corpus into
prompt text for a second reading of it: * every **approved** source-label rule
becomes one glossary line, so a label an engine could not place is placed the
way the engagement already approved; * every independently agreed document
family is named, so a page whose family the intake rules could not decide is
not read as a generic form; * an operator may add reasoning-only prose
describing the business. Three properties make this safe to put in front of an
extraction model: * **Nothing here is a value.** The glossary says where a
printed label belongs, never what it says. The prompt preamble in
``extraction_schema`` states that the page wins over any note, and that an
absent label stays null. * **Only approved rules are used.** An unapproved
mapping proposal is a suggestion the client or engagement owner has not
accepted; putting it in the prompt would apply it to every page without anyone
having approved it. * **Both lanes get identical text.** The context is one
file, referenced by both extraction lanes through ``LLM_CORPUS_CONTEXT``.
``consensus`` refuses a pair of handoffs whose context hashes differ, because
coaching one engine and not the other measures the coaching rather than the
page.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved semantic-mapping registry. Only an approved
                        rule becomes a glossary line; a proposal nobody
                        accepted is not put in front of the model.
  --classifications CLASSIFICATIONS
                        Classification-consensus artifact naming the document
                        families two independent vendors agreed on.
  --notes NOTES         Optional operator prose about the business. Reasoning-
                        only context: it is never evidence and authorizes no
                        value.
  --out OUT             New context file to point LLM_CORPUS_CONTEXT at for
                        both extraction lanes.
  --provenance PROVENANCE
                        New JSON record of what this context was built from,
                        so a later reader can say exactly what conditioned a
                        reading.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `apply_mappings.py`

```text
usage: apply_mappings.py [-h] --registry REGISTRY --out OUT --records-out RECORDS_OUT --exceptions
       EXCEPTIONS [--minimum-engines MINIMUM_ENGINES] [--independent-evidence ARTIFACT]
       [--authorization AUTHORIZATION] [--quiet] consensus templates

Promote source-labelled proposals into controlled fields under approved rules.
An extraction engine that meets a printed label it cannot place in the
controlled vocabulary retains it as a source-labelled proposal rather than
guessing. That is correct, and it is where the value stops: ``consensus.py``
claims no consensus over extension entries, and the approved mapping registry
was read only by the table lanes and by reconciliation. Nothing carried an
approved rule back to the document record that attribution, completeness, and
the inferred controls read. The cost of that gap is concrete. A 716-page
commission corpus printed its acknowledgement numbers under ``ACK NO``, ``ACK
#``, ``ACK#`` and dealer-qualified variants on almost every page, and retained
21,374 source-labelled entries. Zero documents carried
``header.acknowledgement_number``, so the one attribution lane available to a
client with no reference master reported ``no_explicit_attribution_key`` for
715 of 716 documents -- not because the key was missing from the page, but
because no command existed to move it. This command closes that gap without
weakening a control: * Only a rule already carrying an approval (client or
engagement owner) can promote anything. An unapproved proposal is ignored, not
applied. * A promotion is reconciled to the same standard as any other field.
Two independent engines must agree on the value; a lone engine or a
disagreement is retained as an explicit exception rather than promoted. *
Nothing is rewritten. The consensus artifact is read-only input, every
promoted field records the rule, source label, and engines behind it, and the
augmented record set is a new artifact.

positional arguments:
  consensus             Completed consensus artifact whose retained source
                        labels are mapped.
  templates             Source-template observation artifact binding each
                        document to the layout whose fingerprint the registry
                        rules are keyed by.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved semantic-mapping registry. Only an approved
                        rule may promote a value.
  --out OUT             New applied-mapping decision artifact.
  --records-out RECORDS_OUT
                        New record set carrying the accepted promotions in the
                        controlled header, for the controls that read document
                        records rather than table rows.
  --exceptions EXCEPTIONS
                        New exception artifact naming every approved label
                        that could not be promoted. Absence of exceptions is a
                        result, not a formality.
  --minimum-engines MINIMUM_ENGINES
                        Independent engines that must agree on a value before
                        it is promoted into a controlled field. Below two a
                        promotion would rest on a single reading.
  --independent-evidence ARTIFACT
                        Repeatable independent extractor artifact. A label
                        only one engine read, which the independent extractor
                        also read from that page, is promoted as
                        `corroborated_by_independent_extractor` rather than
                        refused. This accepts a controlled value on weaker
                        evidence than vendor agreement, so it requires
                        --authorization.
  --authorization AUTHORIZATION
                        Client decision that independent corroboration of a
                        printed label may stand in for a second engine.
                        Required with --independent-evidence and meaningless
                        without it.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `classification_consensus.py`

```text
usage: classification_consensus.py [-h] --out OUT --exceptions EXCEPTIONS [--minimum-vendors
       MINIMUM_VENDORS] [--quiet] handoffs [handoffs ...]

Accept a document type only where independent model vendors already agreed.
Phase 2 classification is a deterministic phrase-rule pass over the page's own
text. When a client's document family carries no distinctive printed phrase
the rules produce nothing, which is the correct conservative outcome: a
misclassified document extracts *structurally* wrong, not merely inaccurately.
What was missing is the second chance. A 716-page commission corpus rule-
classified 25 pages and escalated 691, and every later lane inherited
``document_family: unknown``: the whole corpus collapsed into one observed
"template" of 2,430 labels, mapping discovery proposed rules against a
fingerprint that stood for nothing, and drift compared layouts that were never
layouts. Meanwhile both extraction engines had independently and correctly
classified those same pages, and their answers sat unused in
``model_document_type`` -- retained the entire time as review proposals
nothing could act on. This command turns that retained evidence into an
explicit, reviewable classification decision. It applies exactly the standard
the rest of the pipeline applies to a fact: two *genuinely independent model
vendors* must agree. A single engine's opinion is never enough, a router does
not create a second vendor, and ``unknown`` is never an agreement -- those
stay explicit exceptions. It is append-only. It reads the retained handoffs
and writes one new artifact; it never edits an extraction record, a manifest,
or a page. Its acceptances are consensus evidence for the *family* of a
document, which is what template observation groups by; they do not re-open
extraction, and they do not clear any control.

positional arguments:
  handoffs              Retained independent extraction handoffs, one per
                        consensus lane. Independence is resolved to the model
                        vendor behind any router, so two lanes reaching one
                        vendor are refused as a single reading.

options:
  -h, --help            show this help message and exit
  --out OUT             New accepted-classification artifact path.
  --exceptions EXCEPTIONS
                        New exception artifact naming every document whose
                        type no independent pair agreed on. Absence of
                        exceptions is a result, not a formality.
  --minimum-vendors MINIMUM_VENDORS
                        Distinct model vendors that must propose the same
                        document type before it is accepted. Below two there
                        is no independent agreement to speak of.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `template_observations.py`

```text
usage: template_observations.py [-h] --out OUT --exceptions EXCEPTIONS [--classifications
       CLASSIFICATIONS] [--quiet] consensus

Build source-template observations from a completed consensus run. Three lanes
consume a source-template artifact -- `schema_discovery.py discover`,
`allocation_policy.py`, and `template_drift.py analyze` -- and nothing in this
repository produced one. Each was documented with an input an operator had to
hand-build, which is how a lane ends up never run. The material now exists.
Consensus retains every printed label an engine could not fit into the
controlled vocabulary, verbatim, under `source_labelled_field_proposals`; on a
real 18-page corpus that was 109 distinct labels across every document. This
command groups documents that show the same labels into one observed template
and writes the artifact those three lanes ask for. What this is not: a
mapping. A label is copied exactly as printed and never renamed, matched, or
interpreted here. Deciding what "Comm. Date Paid" means is
`schema_discovery`'s job, and approving that meaning is the client's. A
template here is one document family, not one document. Grouping on the exact
set of labels a page happened to show produced 18 templates from 18 pages of a
single statement layout, because label capture varies page to page -- between
2 and 47 labels on the trial corpus. That variance is extraction noise, not
layout difference, and a template per page tells drift detection nothing. So
the family's labels are the union of what its documents showed, and each label
carries the number of documents it appeared in. That keeps the variance
visible rather than averaging it away: a label seen in 2 of 18 documents is a
different observation from one seen in all 18, and the artifact says which.
One further limitation is deliberate. Labels are ordered deterministically
rather than in layout order, because the extension channel's occurrence index
reflects the order an engine happened to enumerate in, not the order they
appear on the page. Template drift therefore detects an added or removed
label, which is the case that produces structurally wrong extraction, and not
a pure reordering of the same labels.

positional arguments:
  consensus             consensus.py output whose retained printed labels are
                        grouped

options:
  -h, --help            show this help message and exit
  --out OUT             New source-template observation artifact.
  --exceptions EXCEPTIONS
                        New exception artifact naming every document that
                        contributed no template.
  --classifications CLASSIFICATIONS
                        Accepted classification_consensus_v1 artifact whose
                        independently agreed document types decide the family
                        a document is grouped under. Without it the intake
                        rule classification decides, and a corpus it could not
                        classify groups into one template that is not a
                        layout.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `template_drift.py`

```text
usage: template_drift.py [-h] {analyze,registry-update} ...

Fingerprint source layouts, detect drift, and append authorized registry
entries.

positional arguments:
  {analyze,registry-update}
    analyze             compare source layouts with an approved registry
    registry-update     append operator-authorized template rules

options:
  -h, --help            show this help message and exit
```

## `template_drift.py analyze`

```text
usage: template_drift.py analyze [-h] [--registry REGISTRY] --out OUT templates

positional arguments:
  templates            source-layout observation JSON

options:
  -h, --help           show this help message and exit
  --registry REGISTRY  approved template registry JSON
  --out OUT            new drift analysis JSON
```

## `template_drift.py registry-update`

```text
usage: template_drift.py registry-update [-h] --out OUT registry authorization

positional arguments:
  registry       current approved template registry JSON
  authorization  operator-authorized compiled plan JSON

options:
  -h, --help     show this help message and exit
  --out OUT      new registry snapshot JSON
```

## `llm_adapter.py`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `llm_adapter.py extraction`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `llm_adapter.py consensus_primary`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `llm_adapter.py consensus_secondary`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `llm_adapter.py consensus_tiebreaker`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `llm_adapter.py reasoning`

```text
usage: llm_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--lane
       {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}] [--model
       MODEL] [--credential-env CREDENTIAL_ENV] [--input-mode {auto,text,pdf,image}] [--provider-
       only PROVIDER_ONLY] [--image-dpi IMAGE_DPI] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--max-workers MAX_WORKERS] [--cache-dir CACHE_DIR] [--retry-backoff-
       seconds RETRY_BACKOFF_SECONDS] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--project-id PROJECT_ID] [--location LOCATION] manifest

Extract immutable intake pages with one selected LLM provider.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --lane {extraction,consensus_primary,consensus_secondary,consensus_tiebreaker,reasoning}
                        provider/model lane; consensus lanes are run
                        separately before consensus.py
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --input-mode {auto,text,pdf,image}
                        Submit native page text, the page PDF, a locally
                        rendered page image, or auto to use text only on
                        safely rule-classified pages. 'image' opens the
                        vendors that read documents but do not accept a PDF;
                        auto never selects it, and a provider adapter that has
                        not been verified for a mode refuses it.
  --provider-only PROVIDER_ONLY
                        Comma-separated OpenRouter provider slugs permitted to
                        serve this lane, such as 'coreweave,modal'. OpenRouter
                        otherwise chooses the serving provider itself, and a
                        degraded one returns a truncated stream that reads
                        downstream as a model which could not read the page.
                        Unset routes normally; an unknown slug is refused by
                        OpenRouter, which answers with the providers actually
                        serving the model.
  --image-dpi IMAGE_DPI
                        Resolution of the local page render used by --input-
                        mode image. Retained on each record with the image
                        digest, because the same page at a different
                        resolution is a different reading rather than the same
                        one.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-workers MAX_WORKERS
                        Maximum concurrent in-flight provider requests.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --project-id PROJECT_ID
                        required when LLM_EXTRACT_PROVIDER=google
  --location LOCATION   Vertex region, for example us-central1
```

## `openai_adapter.py`

```text
usage: openai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-
       env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES] [--max-text-chars
       MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--input-mode
       {auto,text,pdf}] manifest

Extract immutable intake pages with the OpenAI Responses API.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list for consensus
  --adapter-out ADAPTER_OUT
                        new provider handoff object for operations.py
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty directory for raw per-page responses
  --model MODEL         client-approved OpenAI vision model
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        explicit model reasoning effort recorded in non-secret
                        run artifacts
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --input-mode {auto,text,pdf}
                        auto uses native text only for rule-classified pages;
                        otherwise use the retained PDF
```

## `openai_adapter.py auto`

```text
usage: openai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-
       env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES] [--max-text-chars
       MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--input-mode
       {auto,text,pdf}] manifest

Extract immutable intake pages with the OpenAI Responses API.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list for consensus
  --adapter-out ADAPTER_OUT
                        new provider handoff object for operations.py
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty directory for raw per-page responses
  --model MODEL         client-approved OpenAI vision model
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        explicit model reasoning effort recorded in non-secret
                        run artifacts
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --input-mode {auto,text,pdf}
                        auto uses native text only for rule-classified pages;
                        otherwise use the retained PDF
```

## `openai_adapter.py text`

```text
usage: openai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-
       env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES] [--max-text-chars
       MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--input-mode
       {auto,text,pdf}] manifest

Extract immutable intake pages with the OpenAI Responses API.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list for consensus
  --adapter-out ADAPTER_OUT
                        new provider handoff object for operations.py
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty directory for raw per-page responses
  --model MODEL         client-approved OpenAI vision model
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        explicit model reasoning effort recorded in non-secret
                        run artifacts
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --input-mode {auto,text,pdf}
                        auto uses native text only for rule-classified pages;
                        otherwise use the retained PDF
```

## `openai_adapter.py pdf`

```text
usage: openai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-
       env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES] [--max-text-chars
       MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--input-mode
       {auto,text,pdf}] manifest

Extract immutable intake pages with the OpenAI Responses API.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list for consensus
  --adapter-out ADAPTER_OUT
                        new provider handoff object for operations.py
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty directory for raw per-page responses
  --model MODEL         client-approved OpenAI vision model
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        explicit model reasoning effort recorded in non-secret
                        run artifacts
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --input-mode {auto,text,pdf}
                        auto uses native text only for rule-classified pages;
                        otherwise use the retained PDF
```

## `google_genai_adapter.py`

```text
usage: google_genai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--model MODEL] [--project-id PROJECT_ID] [--location LOCATION]
       [--credential-env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES]
       [--max-text-chars MAX_TEXT_CHARS] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries
       MAX_RETRIES] [--max-output-tokens MAX_OUTPUT_TOKENS] [--max-workers MAX_WORKERS] [--cache-dir
       CACHE_DIR] [--retry-backoff-seconds RETRY_BACKOFF_SECONDS] [--max-backoff-seconds
       MAX_BACKOFF_SECONDS] [--input-mode {auto,text,pdf}] manifest

Extract immutable intake pages with Gemini on Vertex AI.

positional arguments:
  manifest              ingestion_manifest.json created by
                        scripts/ingest_pages.py

options:
  -h, --help            show this help message and exit
  --out OUT             new normalized per-engine record list
  --adapter-out ADAPTER_OUT
                        new provider handoff object
  --exceptions EXCEPTIONS
                        new provider/schema exception queue
  --raw-dir RAW_DIR     new or empty raw-response directory
  --model MODEL         client-approved Gemini model
  --project-id PROJECT_ID
                        Google Cloud project billed for and serving the
                        request.
  --location LOCATION   Google Cloud region serving the request.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-text-chars MAX_TEXT_CHARS
                        Maximum characters of native page text submitted in
                        one request.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-output-tokens MAX_OUTPUT_TOKENS
                        Output ceiling for one page; a wide table truncates
                        below the model maximum.
  --max-workers MAX_WORKERS
                        Pages read concurrently. A dense page takes minutes,
                        so 1 is hours per hundred.
  --cache-dir CACHE_DIR
                        Directory of retained responses replayed instead of
                        calling the provider. Clear it for a genuinely fresh
                        reading.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay before a retried page request.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Ceiling on that delay, so a failing page cannot stall
                        the lane indefinitely.
  --input-mode {auto,text,pdf}
                        Submit native page text, the page PDF, or auto to use
                        text only on safely rule-classified pages.
```

## `google_document_ai_adapter.py`

```text
usage: google_document_ai_adapter.py [-h] --out OUT --adapter-out ADAPTER_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--project-id
       PROJECT_ID] [--location LOCATION] [--processor-id PROCESSOR_ID] [--processor-version
       PROCESSOR_VERSION] [--credential-env CREDENTIAL_ENV] [--max-pages MAX_PAGES] [--max-pdf-bytes
       MAX_PDF_BYTES] [--timeout-seconds TIMEOUT_SECONDS] [--native-pdf-parsing] manifest

Retain independent Google Document AI OCR/table evidence for intake pages.

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --adapter-out ADAPTER_OUT
                        Destination path for the versioned, hash-bound adapter
                        handoff that downstream controls consume.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated successful raw responses from an
                        interrupted prior attempt.
  --enable              explicitly permit this proposal-only live provider
                        call
  --project-id PROJECT_ID
                        Google Cloud project billed for and serving the
                        request.
  --location LOCATION   Google Cloud region serving the request.
  --processor-id PROCESSOR_ID
                        Document AI processor that serves the request.
  --processor-version PROCESSOR_VERSION
                        Pinned processor version, retained so a reading can be
                        reproduced.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --native-pdf-parsing  Use the processor's native PDF parsing instead of
                        rasterized input.
```

## `google_handwriting_ocr.py`

```text
usage: google_handwriting_ocr.py [-h] [--regions REGIONS] --out OUT --adapter-out ADAPTER_OUT
       --exceptions EXCEPTIONS --raw-dir RAW_DIR --images-dir IMAGES_DIR [--enable] [--project-id
       PROJECT_ID] [--location {global,us,eu}] [--credential-env CREDENTIAL_ENV] [--language-hints
       LANGUAGE_HINTS] [--max-pages MAX_PAGES] [--max-pdf-bytes MAX_PDF_BYTES] [--max-image-bytes
       MAX_IMAGE_BYTES] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--render-
       dpi RENDER_DPI] manifest

Read detector-supplied handwriting regions with Google Cloud Vision OCR.

positional arguments:
  manifest              immutable intake manifest

options:
  -h, --help            show this help message and exit
  --regions REGIONS     handwriting-region artifact; repeatable
  --out OUT             new HTR annotation JSON
  --adapter-out ADAPTER_OUT
                        new non-secret adapter handoff
  --exceptions EXCEPTIONS
                        new exception JSON
  --raw-dir RAW_DIR     new raw-response directory
  --images-dir IMAGES_DIR
                        new retained rendered-image directory
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --project-id PROJECT_ID
                        Google Cloud project billed for and serving the
                        request.
  --location {global,us,eu}
                        Google Cloud region serving the request.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --language-hints LANGUAGE_HINTS
                        Comma-separated language hints passed to the OCR
                        request.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --max-image-bytes MAX_IMAGE_BYTES
                        Maximum bytes for one rendered page image. A larger
                        render is an explicit exception.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --render-dpi RENDER_DPI
                        Resolution used to render each retained page before
                        submission.
```

## `table_comprehension.py`

```text
usage: table_comprehension.py [-h] {profile,rows,audit,mappings,assemble} ...

Create source-native table-comprehension proposals.

positional arguments:
  {profile,rows,audit,mappings,assemble}

options:
  -h, --help            show this help message and exit
```

## `table_comprehension.py profile`

```text
usage: table_comprehension.py profile [-h] --page-id PAGE_ID --out OUT --raw-dir RAW_DIR [--context
       CONTEXT] [--independent-evidence INDEPENDENT_EVIDENCE] [--model MODEL] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--credential-env CREDENTIAL_ENV] [--timeout-seconds
       TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] manifest

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.

options:
  -h, --help            show this help message and exit
  --page-id PAGE_ID     Identifier of the retained page this stage reads.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --context CONTEXT     profile/row proposal JSON for rows or audit
  --independent-evidence INDEPENDENT_EVIDENCE
                        Google Document AI adapter handoff; accepted only by
                        audit after page-hash verification
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
```

## `table_comprehension.py rows`

```text
usage: table_comprehension.py rows [-h] --page-id PAGE_ID --out OUT --raw-dir RAW_DIR [--context
       CONTEXT] [--independent-evidence INDEPENDENT_EVIDENCE] [--model MODEL] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--credential-env CREDENTIAL_ENV] [--timeout-seconds
       TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] manifest

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.

options:
  -h, --help            show this help message and exit
  --page-id PAGE_ID     Identifier of the retained page this stage reads.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --context CONTEXT     profile/row proposal JSON for rows or audit
  --independent-evidence INDEPENDENT_EVIDENCE
                        Google Document AI adapter handoff; accepted only by
                        audit after page-hash verification
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
```

## `table_comprehension.py audit`

```text
usage: table_comprehension.py audit [-h] --page-id PAGE_ID --out OUT --raw-dir RAW_DIR [--context
       CONTEXT] [--independent-evidence INDEPENDENT_EVIDENCE] [--model MODEL] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--credential-env CREDENTIAL_ENV] [--timeout-seconds
       TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] manifest

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.

options:
  -h, --help            show this help message and exit
  --page-id PAGE_ID     Identifier of the retained page this stage reads.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --context CONTEXT     profile/row proposal JSON for rows or audit
  --independent-evidence INDEPENDENT_EVIDENCE
                        Google Document AI adapter handoff; accepted only by
                        audit after page-hash verification
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
```

## `table_comprehension.py mappings`

```text
usage: table_comprehension.py mappings [-h] [--registry REGISTRY] [--propose-mappings] --out OUT
       --exceptions EXCEPTIONS --handoff-out HANDOFF_OUT --raw-dir RAW_DIR [--model MODEL]
       [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-env CREDENTIAL_ENV]
       [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--confidence-floor
       CONFIDENCE_FLOOR] profile

positional arguments:
  profile               Retained table profile the mapping proposals are drawn
                        from.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --propose-mappings    opt in to LLM mapping proposals; client approval is
                        still required
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handoff-out HANDOFF_OUT
                        Destination path for the versioned, hash-bound handoff
                        this lane produces for downstream controls.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --confidence-floor CONFIDENCE_FLOOR
                        Minimum proposal confidence retained. Only an exact
                        client-approved registry rule may map a source label.
```

## `table_comprehension.py assemble`

```text
usage: table_comprehension.py assemble [-h] [--registry REGISTRY] --out OUT --decision-cards
       DECISION_CARDS --quality-summary QUALITY_SUMMARY --exceptions EXCEPTIONS --adapter-out
       ADAPTER_OUT profile rows audit

positional arguments:
  profile               Retained table profile for the page being assembled.
  rows                  Retained source-row proposals for the page being
                        assembled.
  audit                 Retained audit result for the page being assembled.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --decision-cards DECISION_CARDS
                        Destination path for the reviewer decision cards.
  --quality-summary QUALITY_SUMMARY
                        Destination path for the assembly quality summary.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --adapter-out ADAPTER_OUT
                        Destination path for the versioned, hash-bound adapter
                        handoff that downstream controls consume.
```

## `table_comprehension_corpus.py`

```text
usage: table_comprehension_corpus.py [-h] --out OUT [--registry REGISTRY] [--independent-evidence
       INDEPENDENT_EVIDENCE [INDEPENDENT_EVIDENCE ...]] [--client-context CLIENT_CONTEXT] [--resume]
       [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-env
       CREDENTIAL_ENV] [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--max-pages
       MAX_PAGES] [--refinement-passes REFINEMENT_PASSES] [--refinement-tolerance
       REFINEMENT_TOLERANCE] manifests [manifests ...]

Run sequential source-native table comprehension.

positional arguments:
  manifests             one or more immutable intake manifests

options:
  -h, --help            show this help message and exit
  --out OUT             new corpus output directory
  --registry REGISTRY   existing client-approved mapping registry
  --independent-evidence INDEPENDENT_EVIDENCE [INDEPENDENT_EVIDENCE ...]
                        One or more Google Document AI adapter handoffs,
                        supplied only to the audit role. Without it the
                        conditional buddy check records `available: false` and
                        cannot run: a page with no corroboration channel then
                        looks identical to one the check cleared.
  --client-context CLIENT_CONTEXT
                        hash-bound client_review_context_v1; comments are
                        reasoning-only, never evidence
  --resume              Resume a previous run through its verified checkpoint.
                        The checkpoint hashes must match the current inputs.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --max-pages MAX_PAGES
                        Maximum retained pages this invocation may submit.
  --refinement-passes REFINEMENT_PASSES
                        Maximum bounded reread passes over the corpus. A cap,
                        not a quota: a pass covers only pages still
                        unresolved, and none runs when none are.
  --refinement-tolerance REFINEMENT_TOLERANCE
                        Changed rereads tolerated before passes stop. A
                        changed reread is an amendment proposal; 0 stops only
                        when the candidate set is unchanged.
```

## `independent_table_reconcile.py`

```text
usage: independent_table_reconcile.py [-h] --registry REGISTRY --document-ai DOCUMENT_AI
       [DOCUMENT_AI ...] --out OUT --exceptions EXCEPTIONS --adapter-out ADAPTER_OUT source_rows

Reconcile source rows with independent Document AI table evidence.

positional arguments:
  source_rows           Mapped source-native table rows to be compared.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --document-ai DOCUMENT_AI [DOCUMENT_AI ...]
                        One or more Document AI handoffs supplying independent
                        table cells. Repeatable.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --adapter-out ADAPTER_OUT
                        Destination path for the versioned, hash-bound adapter
                        handoff that downstream controls consume.
```

## `layout_aware_extract.py`

```text
usage: layout_aware_extract.py [-h] {page,combine} ...

Create review-only rows for a supplied layout.

positional arguments:
  {page,combine}

options:
  -h, --help      show this help message and exit
```

## `layout_aware_extract.py page`

```text
usage: layout_aware_extract.py page [-h] --page-id PAGE_ID --out OUT --raw-dir RAW_DIR [--model
       MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--credential-env
       CREDENTIAL_ENV] [--max-pdf-bytes MAX_PDF_BYTES] [--timeout-seconds TIMEOUT_SECONDS] [--max-
       retries MAX_RETRIES] [--retry-backoff-seconds RETRY_BACKOFF_SECONDS] manifest layout

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.
  layout                Retained layout artifact this command reads.

options:
  -h, --help            show this help message and exit
  --page-id PAGE_ID     Identifier of the retained page this invocation reads.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
```

## `layout_aware_extract.py combine`

```text
usage: layout_aware_extract.py combine [-h] --out OUT --exceptions EXCEPTIONS --adapter-out
       ADAPTER_OUT layout inputs [inputs ...]

positional arguments:
  layout                Retained layout artifact this command reads.
  inputs                Per-page review-only layout artifacts to combine.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --adapter-out ADAPTER_OUT
                        Destination path for the versioned, hash-bound adapter
                        handoff that downstream controls consume.
```

## `layout_dedup.py`

```text
usage: layout_dedup.py [-h] --unique-out UNIQUE_OUT --duplicates-out DUPLICATES_OUT proposals layout

Create deterministic, review-required deduplication views of LLM display rows.

positional arguments:
  proposals             Row proposals to split into unique and exact-duplicate
                        views.
  layout                Retained layout artifact this command reads.

options:
  -h, --help            show this help message and exit
  --unique-out UNIQUE_OUT
                        Destination path for the deterministic unique-row
                        view.
  --duplicates-out DUPLICATES_OUT
                        Destination path for the exact-duplicate view.
                        Duplicates are retained, never dropped.
```

## `consensus.py`

```text
usage: consensus.py [-h] [--out OUT] [--exceptions EXCEPTIONS] [--handwriting-policy
       {strict,comment_only}] [--quiet] inputs [inputs ...]

Merge multi-engine extractions into consensus.

positional arguments:
  inputs                one JSON file per engine

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handwriting-policy {strict,comment_only}
                        strict gates handwritten fields; comment_only
                        preserves them as non-blocking observations
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `consensus.py strict`

```text
usage: consensus.py [-h] [--out OUT] [--exceptions EXCEPTIONS] [--handwriting-policy
       {strict,comment_only}] [--quiet] inputs [inputs ...]

Merge multi-engine extractions into consensus.

positional arguments:
  inputs                one JSON file per engine

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handwriting-policy {strict,comment_only}
                        strict gates handwritten fields; comment_only
                        preserves them as non-blocking observations
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `consensus.py comment_only`

```text
usage: consensus.py [-h] [--out OUT] [--exceptions EXCEPTIONS] [--handwriting-policy
       {strict,comment_only}] [--quiet] inputs [inputs ...]

Merge multi-engine extractions into consensus.

positional arguments:
  inputs                one JSON file per engine

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handwriting-policy {strict,comment_only}
                        strict gates handwritten fields; comment_only
                        preserves them as non-blocking observations
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `engine_agreement.py`

```text
usage: engine_agreement.py [-h] [--out OUT] [--quiet] reference candidate

Qualify a candidate engine against a reference engine, by value not by count.
A second reader exists to corroborate. Whether it can is a question about the
values it returns, and row counts do not answer it. One run made that concrete
and expensive. A candidate matched the reference engine's row count almost
exactly -- 67 to 67, 54 to 54, 172 rows against 171 over four sample pages --
and on that evidence a 716-page corpus was read. The values told a different
story: the candidate had failed to read 42% of the reference engine's values
anywhere on the page, and placed the right value in the right field 37.5% of
the time. It misread digits inside financial amounts, which is worse than
reading nothing: a wrong amount does not abstain, it disagrees, and every
disagreement it manufactures costs a person an adjudication. So this command
reports two rates that a row count hides: * **value recall** -- how much of
the reference engine's content the candidate read *anywhere* on the page. This
is the reading question, asked without regard to where the candidate filed
what it read. * **field-exact agreement** -- the right value, in the right
field, on the same row. This is the structure question, and it can only be
lower than recall. Read them together. High recall with low field-exact means
the engine can read the page but not the table, and its output will collide
with the reference engine on field identity rather than on content. Low recall
means it cannot read the page, and no amount of prompting downstream will
recover what it never saw. Run this against a handful of matched pages
*before* committing a corpus, and compare only pages both engines actually
read: a rate over a different population is not a comparison. The output is a
measurement, not a decision -- it clears no control and authorizes no lane.

positional arguments:
  reference   Retained handoff of the engine already trusted.
  candidate   Retained handoff of the engine being qualified.

options:
  -h, --help  show this help message and exit
  --out OUT   New agreement artifact path.
  --quiet     Suppress the console summary only. Retained artifacts, findings,
              and exit status are unchanged.
```

## `independent_corroboration.py`

```text
usage: independent_corroboration.py [-h] --out OUT --exceptions EXCEPTIONS [--reference-engine
       REFERENCE_ENGINE] [--quiet] findings evidence [evidence ...]

Corroborate a single reading against an independent extractor's own reading.
Consensus accepts a field where two independent model vendors agreed. Where
only one engine read a field, the finding is `single_engine` -- not a
disagreement, an absence. On one run that was 52,528 findings, 94.9% of them
line-item cells, and no amount of tolerance would close them: nothing had read
the field twice. Something had. Google Document AI ran over the same corpus,
retained its own table evidence, and was never consulted, because
`independent_table_reconcile.py` matches a mapped row against a table cell and
Document AI merges whole logical rows into one cell -- `"12497\nPonte
Gaeda\n33,144.35\n..."` -- so cell-identity matching found nothing. Value
presence finds it. That was a matching defect, not absent evidence. This lane
asks one question per finding: does the value the engine read appear in what
an independent extractor read from the same page? It answers it two ways, and
both are recorded: * a **corroboration** -- one engine read a value and an
independent extractor's own reading of that page contains it; * a **tie-
break** -- two engines disagreed and exactly one of the candidates appears in
that evidence. What this is not ---------------- This is **not vendor
agreement**, and nothing here may be presented as it. Presence proves the
string is printed on that page. It does not prove the string belongs to that
row's column, because the evidence is matched over the page rather than the
cell. It therefore defeats a **misread** and does not defeat a
**misattribution**: an engine that read `5,193.60` correctly but filed it
under the wrong column is corroborated by this lane and still wrong.
`occurrences` is recorded for that reason. A value appearing exactly once on a
page is nearly as located as a cell match; a value appearing twenty times is
weak evidence about which row it belongs to. Read it before treating a
corroboration as settling a field. A short value cannot be matched as a
substring without matching inside longer numbers, so anything under
`MINIMUM_SUBSTRING_LENGTH` characters is matched as a whole token or not at
all. This lane resolves nothing. It writes evidence and exceptions; accepting
a value on that evidence is a separate, authorized step.

positional arguments:
  findings              Consensus exception artifact.
  evidence              Independent extractor artifacts. Pass every one the
                        run retained: a recovery pass commonly covers pages
                        the first pass did not.

options:
  -h, --help            show this help message and exit
  --out OUT             New corroboration artifact path.
  --exceptions EXCEPTIONS
                        New exception artifact path.
  --reference-engine REFERENCE_ENGINE
                        Engine whose readings are being weighed.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `apply_corroboration.py`

```text
usage: apply_corroboration.py [-h] --authorization AUTHORIZATION --out OUT --records-out RECORDS_OUT
       --exceptions EXCEPTIONS [--unique-occurrence-only] [--quiet] records exceptions_in
       corroboration

Accept a corroborated reading under an explicit client authorization.
`independent_corroboration.py` weighs a reading against an independent
extractor's own reading of the same page and deliberately resolves nothing:
presence is page-scoped, so it defeats a **misread** and not a
**misattribution**. Turning that evidence into an accepted value is the
separate authorized step the corroboration artifact says it is, and this is
that step. It runs only against a named client authorization. Without
`--authorization` there is no decision to apply and the lane refuses, because
accepting a value on weaker-than-consensus evidence is a policy choice and
never a default. What acceptance means here -------------------------- * A
**corroborated single reading** -- one engine read a value and an independent
non-LLM extractor read it from that page -- is accepted. * A **tie-break** --
two engines disagreed and exactly one candidate appears in that evidence -- is
accepted as that candidate, and is recorded separately because it began as a
disagreement. Every accepted field keeps `accepted_by:
corroborated_by_independent_extractor`, the authorization it was accepted
under, the match mode, and `occurrences`. It is **never** relabelled
`consensus_*`: no second model vendor read it, and an artifact that claimed
otherwise would launder the weaker evidence into the stronger one. The
original consensus field is retained beside it. The known limit travels with
every acceptance: an engine that read `5,193.60` correctly and filed it under
the wrong column is accepted here and still wrong. `occurrences` says how
located the match was, and `--unique-occurrence-only` restricts acceptance to
values that appear exactly once on their page.

positional arguments:
  records               Consensus record artifact to accept into.
  exceptions_in         Consensus exception artifact to reduce.
  corroboration         Independent corroboration artifact.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Client decision this acceptance is made under.
                        Required: accepting a value on page-scoped independent
                        evidence rather than model vendor agreement is a
                        policy choice and never a default.
  --out OUT             New applied-corroboration artifact path.
  --records-out RECORDS_OUT
                        New accepted record set path.
  --exceptions EXCEPTIONS
                        New exception artifact holding every finding this
                        acceptance leaves open.
  --unique-occurrence-only
                        Accept only values that appear exactly once on their
                        page. A value seen once is nearly as located as a cell
                        match; one seen twenty times says little about which
                        row it belongs to.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `arithmetic_check.py`

```text
usage: arithmetic_check.py [-h] [--out OUT] [--tolerance TOLERANCE] [--classification
       CLASSIFICATION] [--failures-only] [--defer-unassigned-pages] [--not-provable-as-not-
       applicable AUTHORIZATION] [--quiet] input

Prove documents against their own arithmetic.

positional arguments:
  input                 JSON: a record, a list of records, or consensus.json

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --tolerance TOLERANCE
                        absolute tolerance per check (default 0.01)
  --classification CLASSIFICATION
                        Accepted classification artifact. An engine handoff
                        carries the intake guess, which is 'unknown' wherever
                        intake found no high-signal type, so without this a
                        commission document is never recognised as one and the
                        only arithmetic it supports is never run.
  --failures-only       Emit only records that failed a check. The retained
                        result set is unchanged.
  --defer-unassigned-pages
                        defer page-level proof until evidence-backed document
                        reassembly exists
  --not-provable-as-not-applicable AUTHORIZATION
                        Count every not_provable document as not_applicable,
                        under the named operator authorization. Each keeps
                        prior_arithmetic_status and the authorization's name;
                        a failed or partly proved document is not touched.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `multi_engine_vote.py`

```text
usage: multi_engine_vote.py [-h] --out OUT --exceptions EXCEPTIONS [--records-out RECORDS_OUT]
       [--minimum-vendors MINIMUM_VENDORS] [--tiebreak-with ARTIFACT] [--pages MANIFEST] [--quiet]
       records handoffs [handoffs ...]

Settle an open field by counting every reading the run already retained.
`consensus.py` compares exactly two lanes. A run that read its corpus with
four vendors therefore judged every field on one pair and never consulted the
other two readings, which are sitting in the run's own provider artifacts. On
the commission run that left 13,423 fields open while 81,460 cells had two or
more vendors on them. Scored against held-out ground truth -- 293 cells an
independent arithmetic proof had already settled, which this lane never sees
-- the vote reads: | vendors agreeing | cells | correct | |---|---|---| |
three | 61 | 100% | | two | 188 | 96.3% | For comparison, asking a model to
re-read those same pages and adjudicate them scored 87.1% and cost real money.
The readings were already bought. What keeps this honest: * **One vote per
vendor, not per lane.** Two Google lanes are one reading of the page by one
vendor's weights. `runtime_config.model_vendor` resolves the vendor through a
router, so a lane routed to `google/gemini` and a lane on Vertex count once
between them. Counting lanes would manufacture exactly the independence this
repository exists to protect. * **An accepted value is never overruled.** This
settles open fields only. * **A tie is not a result.** Where the leading value
does not lead outright the field stays open and is counted, because a coin-
flip between two vendors is the finding, not a resolution. * **The agreement
count rides on the acceptance.** Two vendors agreeing and three vendors
agreeing are different evidence -- 96.3% against 100% here -- and a reader
must be able to tell them apart afterwards.

positional arguments:
  records               Retained record artifact whose open fields are voted
                        on.
  handoffs              Retained provider handoffs. Pass every reading the run
                        holds: each vendor votes once however many lanes it
                        ran, and a vendor the run never read with cannot be
                        counted.

options:
  -h, --help            show this help message and exit
  --out OUT             New vote artifact path.
  --exceptions EXCEPTIONS
                        New unresolved-field artifact path.
  --records-out RECORDS_OUT
                        Apply the resolutions to a new record artifact.
  --minimum-vendors MINIMUM_VENDORS
                        How many independent vendors must read the same value.
                        Below two there is no agreement to speak of, and the
                        measured accuracy differs by band: two vendors scored
                        96.3% against held-out truth and three scored 100%.
  --tiebreak-with ARTIFACT
                        Repeatable non-LLM extractor artifact. Where the
                        vendors tie, a reading the extractor read off that
                        page and the other did not breaks it. Labelled
                        tie_broken_by_independent_extractor, never vendor
                        agreement: presence proves the string is printed on
                        the page, not that it belongs in that cell.
  --pages MANIFEST      Repeatable ingestion manifest of a subset read. Only
                        the documents on its pages are voted on and counted;
                        every other document passes to --records-out
                        unchanged. Use it when the handoffs read only those
                        pages.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `arithmetic_reconcile.py`

```text
usage: arithmetic_reconcile.py [-h] --out OUT --exceptions EXCEPTIONS [--records-out RECORDS_OUT]
       [--relative-tolerance RELATIVE_TOLERANCE] [--tolerance TOLERANCE] [--rate-tolerance
       RATE_TOLERANCE] [--quiet] records

Let a document's own arithmetic choose between two readings of a cell.
Consensus accepts a value two vendors agree on. Where they disagree it records
`no_consensus` and the cell stays open -- 9,007 of them on the commission run,
2,551 carrying money. Nothing downstream can settle those, because both
readings are equally attested: one engine said one thing, one said another,
and page presence cannot separate them. `independent_corroboration.py` proves
a string is printed somewhere on the page, which defeats a misread and not a
misplacement. A commission line states its own arithmetic. `commission_amount`
is `commissionable_amount` times `stated_commission_rate`, and where two of
the three are known the third is not a matter of opinion. This lane computes
it and asks one question of the candidate readings: does exactly one of them
satisfy the line? That is evidence from the document, not a model's
preference, and it is the only thing here that can defeat a **misplacement**.
It caught one on the first page it ran. Two engines offered `(662.89)` and
`4,098.49` for one line's commission, and `4,098.49` appeared again as a
candidate on a different line of the same page -- the second engine had read
the table one row off. Neither reading was wrong about the number; one was
wrong about the row. The arithmetic puts each back where it belongs.
Deliberately narrow: * Only a cell consensus left open is considered. An
accepted value is evidence and this does not overrule it. * A sibling supplies
a value only when it is accepted, or when it is open with exactly one
candidate. Two competing siblings would make the expected value a choice, and
choosing it to justify a choice is circular. * Exactly one candidate must
match. Two matches is an ambiguity, not a resolution, and zero matches means
the readings disagree with the line -- both are reported rather than resolved.
* Resolutions are proposals. `--records-out` applies them, labelled
`reconciled_by_document_arithmetic`, never `consensus_*`: an arithmetic proof
is not two vendors agreeing and must never be counted as it.

positional arguments:
  records               Retained record artifact carrying line cells.

options:
  -h, --help            show this help message and exit
  --out OUT             New reconciliation artifact path.
  --exceptions EXCEPTIONS
                        New unresolved-cell artifact path.
  --records-out RECORDS_OUT
                        Apply the resolutions to a new record artifact. Each
                        amended cell is labelled
                        reconciled_by_document_arithmetic and never
                        consensus_*.
  --relative-tolerance RELATIVE_TOLERANCE
                        Fraction of the expected value a candidate may differ
                        by. Rounding in a source document is proportional, so
                        the tolerance is too; a flat amount reads a rounded
                        total on a large line as a contradiction.
  --tolerance TOLERANCE
                        Absolute floor for the match width, in currency units.
                        It governs small values, where a proportional
                        tolerance collapses to nothing.
  --rate-tolerance RATE_TOLERANCE
                        Match width for a commission rate, in percentage
                        points. A rate is quoted in whole percent and rounds
                        harder than a currency amount, so it keeps an absolute
                        width rather than a proportional one.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `adjudicate.py`

```text
usage: adjudicate.py [-h] --out OUT --exceptions EXCEPTIONS [--max-passes {1,2}] [--quiet] proofed
       sources [sources ...]

Propose bounded arithmetic-backed amendments without changing evidence.

positional arguments:
  proofed               proofed.json from scripts/arithmetic_check.py
  sources               independent raw engine JSON files

options:
  -h, --help            show this help message and exit
  --out OUT             amendment proposal JSON
  --exceptions EXCEPTIONS
                        adjudication exception JSON
  --max-passes {1,2}    Maximum adjudication passes. Two passes may propose an
                        amendment; one records candidates only.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `validate_extraction.py`

```text
usage: validate_extraction.py [-h] --out OUT --exceptions EXCEPTIONS [--currencies CURRENCIES]
       [--engagement-party NAME] [--quiet] input

Validate extracted fields without altering source values.

positional arguments:
  input                 consensus/proofed JSON, a record, or a list of records

options:
  -h, --help            show this help message and exit
  --out OUT             records with validation findings
  --exceptions EXCEPTIONS
                        client-review validation exceptions
  --currencies CURRENCIES
                        comma-separated allowed ISO currency codes
  --engagement-party NAME
                        This engagement's own party name, repeatable. A
                        representative agency appears on every page of its own
                        commission statements, and an extractor reads that
                        name into brand_name as readily as the manufacturer's;
                        naming it here routes those to review instead of
                        letting the agency become the dataset's largest
                        supplier.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `address_normalize.py`

```text
usage: address_normalize.py [-h] --out OUT --exceptions EXCEPTIONS [--google-address-validation |
       --no-google-address-validation] [--google-api-key-env GOOGLE_API_KEY_ENV] [--max-google-
       requests MAX_GOOGLE_REQUESTS] [--google-timeout-seconds GOOGLE_TIMEOUT_SECONDS] [--google-
       usps-cass | --no-google-usps-cass] [--quiet] input

Derive CRM address components and optional Google validation evidence.

positional arguments:
  input                 validated JSON, a record, or a list of records

options:
  -h, --help            show this help message and exit
  --out OUT             records with address-normalization evidence
  --exceptions EXCEPTIONS
                        address findings for client review
  --google-address-validation, --no-google-address-validation
                        explicitly send addresses to Google Maps Address
                        Validation
  --google-api-key-env GOOGLE_API_KEY_ENV
                        environment-variable name containing the Google Maps
                        API key
  --max-google-requests MAX_GOOGLE_REQUESTS
                        explicit per-run cap from 1 to 5000; default 0
                        disables calls
  --google-timeout-seconds GOOGLE_TIMEOUT_SECONDS
                        Per-request timeout, in seconds, for the Address
                        Validation call.
  --google-usps-cass, --no-google-usps-cass
                        request USPS CASS evidence for US/PR addresses
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `handwriting_review.py`

```text
usage: handwriting_review.py [-h] --out OUT --exceptions EXCEPTIONS [--max-passes {1,2}] [--quiet]
       runs [runs ...]

Reconcile independent HTR runs without mutating source values.

positional arguments:
  runs                  independent HTR JSON result files

options:
  -h, --help            show this help message and exit
  --out OUT             accepted-reading and amendment-proposal JSON
  --exceptions EXCEPTIONS
                        final client-review handwriting queue JSON
  --max-passes {1,2}    Maximum reconciliation passes. Two passes may propose
                        an amendment; one records readings only.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `crm_input_validate.py`

```text
usage: crm_input_validate.py [-h] --grain NAME=PATH --schema SCHEMA --out OUT [--findings-csv
       FINDINGS_CSV] [--quiet]

Validate an exported CRM grain against the canonical schema it loads into.

options:
  -h, --help            show this help message and exit
  --grain NAME=PATH     exported CSV to validate, repeatable (for example
                        lines=.../lines.csv)
  --schema SCHEMA       canonical DDL naming the target columns
  --out OUT             validation report
  --findings-csv FINDINGS_CSV
                        the same findings as a CSV an operator can work
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `specifier_recover.py`

```text
usage: specifier_recover.py [-h] --extractor-raw EXTRACTOR_RAW --out OUT --exceptions EXCEPTIONS
       [--quiet] input

Recover the printed SPECIFIER column from retained extractor layout.

positional arguments:
  input                 records JSON to carry forward

options:
  -h, --help            show this help message and exit
  --extractor-raw EXTRACTOR_RAW
                        directory of retained independent-extractor responses
  --out OUT             records with recovered specifiers
  --exceptions EXCEPTIONS
                        pages the recovery could not settle
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `brand_recover.py`

```text
usage: brand_recover.py [-h] --extractor-raw EXTRACTOR_RAW --out OUT --exceptions EXCEPTIONS
       [--client-name NAME] [--quiet] input

Recover the manufacturer from the letterhead the page printed.

positional arguments:
  input                 records JSON to carry forward

options:
  -h, --help            show this help message and exit
  --extractor-raw EXTRACTOR_RAW
                        directory of retained independent-extractor responses
  --out OUT             records with recovered brands
  --exceptions EXCEPTIONS
                        pages the recovery could not settle
  --client-name NAME    the party whose records these are, repeatable. It is
                        not a manufacturer, and on a billing report that
                        prints it as the agent of record it is exactly what
                        `brand_name` wrongly held.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `page_review.py`

```text
usage: page_review.py [-h] --lines-csv LINES_CSV --documents-csv DOCUMENTS_CSV [--images IMAGES]
       [--packet PACKET] [--reviews REVIEWS] [--out OUT] [--proposals PROPOSALS] [--fields-csv
       FIELDS_CSV] [--extractor-raw EXTRACTOR_RAW] [--second-read SECOND_READ] [--reviewer-vendor
       REVIEWER_VENDOR] [--quiet]

Verify what the export says against what the page prints, page by page.

options:
  -h, --help            show this help message and exit
  --lines-csv LINES_CSV
                        Delivery line-grain CSV.
  --documents-csv DOCUMENTS_CSV
                        Delivery document-grain CSV.
  --images IMAGES       Directory of page images, for --packet.
  --packet PACKET       Emit the review packet for this page id.
  --reviews REVIEWS     Directory of returned review JSON files.
  --out OUT             Write the summary here as JSON.
  --proposals PROPOSALS
                        Also write what the reviews propose, graded by
                        evidence, here as JSON.
  --fields-csv FIELDS_CSV
                        Delivery field-grain CSV, for every reading each
                        engine returned.
  --extractor-raw EXTRACTOR_RAW
                        Retained independent-extractor responses, for the
                        page's own text.
  --second-read SECOND_READ
                        Retained provider handoff of another vendor's reading
                        of the reviewed pages. A correction the reviewer alone
                        supports becomes second_vendor where that reading
                        holds the figure in its column more often than the
                        export's other lines; a row the extractor prints as
                        often as the export becomes
                        second_vendor_prints_it_fewer_times where the reading
                        holds every other figure in the column as often and
                        this one fewer times. Needs --proposals and
                        --reviewer-vendor.
  --reviewer-vendor REVIEWER_VENDOR
                        The model vendor that wrote the reviews; a second read
                        by that vendor is refused.
  --quiet               Suppress the printed summary.
```

## `augment_export.py`

```text
usage: augment_export.py [-h] --lines-csv LINES_CSV --documents-csv DOCUMENTS_CSV --out-lines
       OUT_LINES --out-documents OUT_DOCUMENTS --out OUT [--records RECORDS] [--reviews REVIEWS]
       [--external EXTERNAL] [--client-name CLIENT_NAME] [--parties PARTIES] [--out-accounts
       OUT_ACCOUNTS] [--out-contacts OUT_CONTACTS] [--product-rules PRODUCT_RULES] [--out-products
       OUT_PRODUCTS] [--extractor-raw EXTRACTOR_RAW] [--quiet]

Fill what the export leaves empty from what the run already knows, labelled.

options:
  -h, --help            show this help message and exit
  --lines-csv LINES_CSV
                        Delivery line-grain CSV to fill.
  --documents-csv DOCUMENTS_CSV
                        Delivery document-grain CSV.
  --out-lines OUT_LINES
                        New line-grain CSV with inferences.
  --out-documents OUT_DOCUMENTS
                        New document-grain CSV.
  --out OUT             Summary JSON of what was filled, and how.
  --records RECORDS     Record artifact, for the engines' document types.
  --reviews REVIEWS     Page-review directory, for each page's reviewed kind.
  --external EXTERNAL   Operator-compiled public-source party facts
                        (party_public_sources_v1).
  --client-name CLIENT_NAME
                        The client whose records these are; never filled in as
                        a party. Repeatable.
  --parties PARTIES     Party master from entity_resolve.py. With it the
                        summary counts the accounts and contacts the grains
                        name, and --out-accounts and --out-contacts can be
                        written.
  --out-accounts OUT_ACCOUNTS
                        New accounts CSV: one row per resolved party the
                        grains name as a party -- never the client, a field
                        holding the job, or a reading the resolver refused.
  --out-contacts OUT_CONTACTS
                        New contacts CSV: one row per person the person fields
                        name, one person's spellings merged on evidence, and a
                        company only where an email naming the person proves
                        it.
  --product-rules PRODUCT_RULES
                        Operator rules naming, per manufacturer, the printed
                        column holding its product code and name, or that it
                        lists no products (product_rules_v1). Needs --parties.
                        Each line then carries product__code, product__name,
                        product__variant, product__brand, product__brand_key
                        and product__rule.
  --out-products OUT_PRODUCTS
                        New products CSV: one row per manufacturer and printed
                        product code, from the lines a product rule reads --
                        never a project, a total, a label or a party.
  --extractor-raw EXTRACTOR_RAW
                        Directory of retained independent-extractor responses.
                        With a product rule that checks the printed row, each
                        line's code is read off the row its own amounts are
                        printed on, and product__printed_row_check says
                        whether the engines' code agrees. Needs --product-
                        rules.
  --quiet               Suppress the printed summary.
```

## `page_review_amend.py`

```text
usage: page_review_amend.py [-h] --authorization AUTHORIZATION [--evidence
       {extractor_text_and_engine,extractor_text,engine,second_vendor}] --out OUT --report REPORT
       [--quiet] records proposals

Amend what the page review proved, under an operator authorization.

positional arguments:
  records               Record artifact to amend.
  proposals             Proposals artifact from page_review.py --proposals.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Name of the operator authorization the amendments are
                        applied under.
  --evidence {extractor_text_and_engine,extractor_text,engine,second_vendor}
                        Evidence grade to apply; repeatable. Defaults to every
                        grade independent of the reviewer.
  --out OUT             Amended record artifact (a new file).
  --report REPORT       What was amended, and what was not.
  --quiet               Suppress the printed summary.
```

## `page_review_amend.py extractor_text_and_engine`

```text
usage: page_review_amend.py [-h] --authorization AUTHORIZATION [--evidence
       {extractor_text_and_engine,extractor_text,engine,second_vendor}] --out OUT --report REPORT
       [--quiet] records proposals

Amend what the page review proved, under an operator authorization.

positional arguments:
  records               Record artifact to amend.
  proposals             Proposals artifact from page_review.py --proposals.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Name of the operator authorization the amendments are
                        applied under.
  --evidence {extractor_text_and_engine,extractor_text,engine,second_vendor}
                        Evidence grade to apply; repeatable. Defaults to every
                        grade independent of the reviewer.
  --out OUT             Amended record artifact (a new file).
  --report REPORT       What was amended, and what was not.
  --quiet               Suppress the printed summary.
```

## `page_review_amend.py extractor_text`

```text
usage: page_review_amend.py [-h] --authorization AUTHORIZATION [--evidence
       {extractor_text_and_engine,extractor_text,engine,second_vendor}] --out OUT --report REPORT
       [--quiet] records proposals

Amend what the page review proved, under an operator authorization.

positional arguments:
  records               Record artifact to amend.
  proposals             Proposals artifact from page_review.py --proposals.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Name of the operator authorization the amendments are
                        applied under.
  --evidence {extractor_text_and_engine,extractor_text,engine,second_vendor}
                        Evidence grade to apply; repeatable. Defaults to every
                        grade independent of the reviewer.
  --out OUT             Amended record artifact (a new file).
  --report REPORT       What was amended, and what was not.
  --quiet               Suppress the printed summary.
```

## `page_review_amend.py engine`

```text
usage: page_review_amend.py [-h] --authorization AUTHORIZATION [--evidence
       {extractor_text_and_engine,extractor_text,engine,second_vendor}] --out OUT --report REPORT
       [--quiet] records proposals

Amend what the page review proved, under an operator authorization.

positional arguments:
  records               Record artifact to amend.
  proposals             Proposals artifact from page_review.py --proposals.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Name of the operator authorization the amendments are
                        applied under.
  --evidence {extractor_text_and_engine,extractor_text,engine,second_vendor}
                        Evidence grade to apply; repeatable. Defaults to every
                        grade independent of the reviewer.
  --out OUT             Amended record artifact (a new file).
  --report REPORT       What was amended, and what was not.
  --quiet               Suppress the printed summary.
```

## `page_review_amend.py second_vendor`

```text
usage: page_review_amend.py [-h] --authorization AUTHORIZATION [--evidence
       {extractor_text_and_engine,extractor_text,engine,second_vendor}] --out OUT --report REPORT
       [--quiet] records proposals

Amend what the page review proved, under an operator authorization.

positional arguments:
  records               Record artifact to amend.
  proposals             Proposals artifact from page_review.py --proposals.

options:
  -h, --help            show this help message and exit
  --authorization AUTHORIZATION
                        Name of the operator authorization the amendments are
                        applied under.
  --evidence {extractor_text_and_engine,extractor_text,engine,second_vendor}
                        Evidence grade to apply; repeatable. Defaults to every
                        grade independent of the reviewer.
  --out OUT             Amended record artifact (a new file).
  --report REPORT       What was amended, and what was not.
  --quiet               Suppress the printed summary.
```

## `column_refile.py`

```text
usage: column_refile.py [-h] --rules RULES --out OUT --report REPORT [--quiet] records

Re-file a column read into the wrong field, under a named operator
authorization.

positional arguments:
  records          Record artifact whose readings are re-filed.

options:
  -h, --help       show this help message and exit
  --rules RULES    JSON naming the authorization and each re-file: pages,
                   from, to, evidence.
  --out OUT        Re-filed record artifact (a new file).
  --report REPORT  What moved, what did not, and why.
  --quiet          Suppress the printed summary.
```

## `records_in_step.py`

```text
usage: records_in_step.py [-h] [--out OUT] [--report REPORT] [--check] [--quiet] records

Bring a record artifact's fields and its header and line views into step.

positional arguments:
  records          Record artifact whose fields and views are compared.

options:
  -h, --help       show this help message and exit
  --out OUT        The record artifact with its fields and views in step (a
                   new file).
  --report REPORT  What was out of step, and what was carried or kept (a new
                   file).
  --check          Write no records; exit 1 when any document is out of step.
  --quiet          Suppress the printed summary.
```

## `accuracy_sample.py`

```text
usage: accuracy_sample.py [-h] --documents DOCUMENTS --lines LINES [--stratify-by STRATIFY_BY]
       [--unstratified-label UNSTRATIFIED_LABEL] [--per-stratum PER_STRATUM] [--value-column
       VALUE_COLUMN] [--images IMAGES] --out OUT [--worksheet WORKSHEET] [--quiet]

Draw the pages to read against their own image, with the weight each carries.

options:
  -h, --help            show this help message and exit
  --documents DOCUMENTS
                        exported document grain
  --lines LINES         exported line grain
  --stratify-by STRATIFY_BY
                        the document field that separates one page layout from
                        another. Every defect found by reading on this corpus
                        was layout-specific, so a sample that does not
                        stratify by layout measures the wrong thing.
  --unstratified-label UNSTRATIFIED_LABEL
                        the stratum documents with no value in that field
                        belong to
  --per-stratum PER_STRATUM
                        pages to draw per stratum
  --value-column VALUE_COLUMN
                        the money column each stratum's weight is measured in
  --images IMAGES       directory of retained page images
  --out OUT             the sample plan
  --worksheet WORKSHEET
                        the same pages as a CSV a reader can work
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `entity_resolve.py`

```text
usage: entity_resolve.py [-h] [--out OUT] [--log LOG] [--threshold THRESHOLD] [--review-band
       REVIEW_BAND] [--canonical-name NAME] [--absorb-ocr-variants RATIO] [--client-name NAME]
       [--party-decisions PATH] [--quiet] input

Resolve party names into a deduplicated master.

positional arguments:
  input                 JSON records, or consensus.json

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --log LOG             reversible merge log
  --threshold THRESHOLD
                        merge at or above this similarity (default 0.86)
  --review-band REVIEW_BAND
                        flag for adjudication at or above this (default 0.72)
  --canonical-name NAME
                        Declare the canonical spelling for a party,
                        repeatable. Matched to a cluster ignoring case and
                        whitespace, so it need not reproduce a variant
                        exactly. Spelling authority is the engagement's, not a
                        frequency count's: one supplier appeared as 'murb
                        rook' on 90 documents and 'Murbrook' on 35.
  --absorb-ocr-variants RATIO
                        Absorb a party whose name is one character from a far
                        more common one, or whose every word only that one
                        name also carries, when the common name has at least
                        RATIO times its mentions. Runs after --party-decisions
                        and never joins a pair a decision keeps apart, or a
                        branch to its parent. Off by default: the standing
                        rule reports a one-character difference rather than
                        merging it, because merging on a single character is
                        how a separate subsidiary is absorbed into its parent.
                        Every absorption is retained with both counts and the
                        ratio that justified it.
  --client-name NAME    the party whose records these are, repeatable. It is
                        printed on nearly every page -- as a statement's
                        addressee, as the agent of record, in the letterhead
                        -- and none of that makes it a counterparty. Left
                        unset, the client becomes the largest account in the
                        resolved master and appears as its own customer,
                        dealer and brand. Matched on the normalized name and
                        on a truncation of it, so one spelling covers the cut-
                        off forms the corpus prints. Every refusal is retained
                        as an exception with the field and document it came
                        from, never dropped.
  --party-decisions PATH
                        JSON naming the authorization it rests on and a list
                        of decisions, each {decision, a, b, evidence,
                        canonical_name}: same_party folds one party into the
                        other, branch_of keeps b as its own account under
                        parent a, different_parties keeps them apart,
                        not_a_party removes a project or site printed in a
                        party column and retains it. A decided pair leaves
                        pending adjudication with its score kept; a decision
                        naming no party is reported.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `party_locations.py`

```text
usage: party_locations.py [-h] --out OUT [--enabled | --no-enabled] [--auth {key,adc}] [--api-key-
       env API_KEY_ENV] [--max-requests MAX_REQUESTS] [--timeout-seconds TIMEOUT_SECONDS] [--context
       CONTEXT] [--skip-known SKIP_KNOWN] [--countries COUNTRIES] [--from-lookups FROM_LOOKUPS]
       [--quiet] parties

Find where a dealer or brand trades, from Google Places, as a proposal.

positional arguments:
  parties               Party master from entity_resolve.py.

options:
  -h, --help            show this help message and exit
  --out OUT             Candidate and accepted locations (a new file).
  --enabled, --no-enabled
                        Allow Places requests for this run. Off unless enabled
                        here or in the environment.
  --auth {key,adc}      Credential: the restricted Places key, or Application
                        Default Credentials.
  --api-key-env API_KEY_ENV
                        Name of the environment variable holding the Places
                        key, when --auth is key.
  --max-requests MAX_REQUESTS
                        Most Places requests this run may make; one request
                        per party, no retries.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request timeout, in seconds.
  --context CONTEXT     Words added to every query, such as the trade.
  --skip-known SKIP_KNOWN
                        A party_public_sources_v1 file; parties whose names it
                        covers are not looked up.
  --countries COUNTRIES
                        Comma-separated ISO 3166-1 alpha-2 codes a party may
                        trade in; a candidate elsewhere is kept, never
                        accepted.
  --from-lookups FROM_LOOKUPS
                        A previous run's output; decide again from its
                        retained candidates, sending nothing.
  --quiet               Suppress the printed summary.
```

## `attribution.py`

```text
usage: attribution.py [-h] [--reference REFERENCE] [--out OUT] [--register REGISTER] [--key-field
       FIELD] [--ack-pattern ACK_PATTERN] [--job-pattern JOB_PATTERN] [--dispositions DISPOSITIONS]
       [--credit-by-line AUTHORIZATION] [--quiet] input

Resolve ACK/job attribution for every dollar.

positional arguments:
  input                 proofed.json, consensus.json, or a record list

options:
  -h, --help            show this help message and exit
  --reference REFERENCE
                        ACK/job reference table (CSV or JSON). Strongly
                        recommended.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --register REGISTER   Destination path for the unattributable register
                        recording every unresolved amount with its reason.
  --key-field FIELD     Header field carrying the attribution key, repeatable
                        and tried in the order given. Defaults to the schema's
                        own key fields: acknowledgement_number, job_number,
                        project_number, sales_order_number, work_order_number,
                        contract_number, ack_number.
  --ack-pattern ACK_PATTERN
                        regex for valid ACK numbers
  --job-pattern JOB_PATTERN
                        regex for valid job numbers
  --dispositions DISPOSITIONS
                        JSON map of document_id -> reason code for known non-
                        job dollars
  --credit-by-line AUTHORIZATION
                        Apply the operator-authorized crediting rule, naming
                        its authorization. A document no rank attributes,
                        whose every line carrying money prints a key, is
                        registered credited_by_line with each line's key and
                        amount -- an answer, not a failure. A document with a
                        money line printing no key stays unresolved.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `allocation_policy.py`

```text
usage: allocation_policy.py [-h] {apply,discover,decision-template,registry-update} ...

Apply review-safe commission allocation policies.

positional arguments:
  {apply,discover,decision-template,registry-update}

options:
  -h, --help            show this help message and exit
```

## `allocation_policy.py apply`

```text
usage: allocation_policy.py apply [-h] --registry REGISTRY --out OUT --exceptions EXCEPTIONS input

positional arguments:
  input                 Attributed-record artifact whose legs the approved
                        policy is applied to.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
```

## `allocation_policy.py discover`

```text
usage: allocation_policy.py discover [-h] [--registry REGISTRY] --out OUT --exceptions EXCEPTIONS
       --handoff-out HANDOFF_OUT --raw-dir RAW_DIR [--enable] [--model MODEL] [--reasoning-effort
       REASONING_EFFORT] input

positional arguments:
  input                 Source-template observation artifact scanned for
                        candidate effective-dated policy rules. Build it with
                        template_observations.py; the attributed-record
                        artifact belongs to `apply`, and is refused here.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handoff-out HANDOFF_OUT
                        Destination path for the versioned, hash-bound handoff
                        this lane produces for downstream controls.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort REASONING_EFFORT
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `allocation_policy.py decision-template`

```text
usage: allocation_policy.py decision-template [-h] --out OUT discovery

positional arguments:
  discovery   Retained discovery-proposal artifact this command reads.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `allocation_policy.py registry-update`

```text
usage: allocation_policy.py registry-update [-h] --out OUT registry discovery decisions

positional arguments:
  registry    Existing approved allocation-policy registry to amend.
  discovery   Retained discovery-proposal artifact this command reads.
  decisions   Client decision artifact naming which discovered rules are
              approved.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `schema_discovery.py`

```text
usage: schema_discovery.py [-h] {discover,slot-equivalence,registry-update} ...

Create review-required semantic schema proposals.

positional arguments:
  {discover,slot-equivalence,registry-update}

options:
  -h, --help            show this help message and exit
```

## `schema_discovery.py discover`

```text
usage: schema_discovery.py discover [-h] [--registry REGISTRY] --out OUT --exceptions EXCEPTIONS
       --handoff-out HANDOFF_OUT --raw-dir RAW_DIR [--provider {anthropic,google,openai,openrouter}]
       [--graph-schema-inventory GRAPH_SCHEMA_INVENTORY] [--enable] [--model MODEL] [--buddy | --no-
       buddy] [--buddy-provider {anthropic,google,openai,openrouter}] [--buddy-model BUDDY_MODEL]
       [--reasoning-effort REASONING_EFFORT] [--min-confidence MIN_CONFIDENCE] [--google-places |
       --no-google-places] [--google-api-key-env GOOGLE_API_KEY_ENV] [--max-google-requests
       MAX_GOOGLE_REQUESTS] [--google-timeout-seconds GOOGLE_TIMEOUT_SECONDS] input

positional arguments:
  input                 Source-template observation artifact whose printed
                        labels are scanned for candidate semantic mappings.
                        Build it with template_observations.py; an extracted-
                        record or consensus artifact is refused.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handoff-out HANDOFF_OUT
                        Destination path for the versioned, hash-bound handoff
                        this lane produces for downstream controls.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --provider {anthropic,google,openai,openrouter}
                        Provider serving this lane. Independence is resolved
                        to the model vendor behind any router.
  --graph-schema-inventory GRAPH_SCHEMA_INVENTORY
                        Proposal-only evidence_graph.py schema output; source
                        templates remain required mapping evidence.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --buddy, --no-buddy   Run the independent buddy pass (--no-buddy skips it).
                        This is an on/off switch, not a provider name -- the
                        buddy provider itself comes from --buddy-provider.
  --buddy-provider {anthropic,google,openai,openrouter}
                        Provider for the buddy lane. It must differ from the
                        primary provider.
  --buddy-model BUDDY_MODEL
                        Model identifier for the buddy lane.
  --reasoning-effort REASONING_EFFORT
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --min-confidence MIN_CONFIDENCE
                        Minimum proposal confidence retained. Client approval
                        is still required for every mapping.
  --google-places, --no-google-places
                        Enable Google Places candidate lookups. Candidates are
                        proposals, never an approved mapping.
  --google-api-key-env GOOGLE_API_KEY_ENV
                        Name of the environment variable holding the Places
                        API key.
  --max-google-requests MAX_GOOGLE_REQUESTS
                        Maximum Places requests this invocation may make.
  --google-timeout-seconds GOOGLE_TIMEOUT_SECONDS
                        Per-request timeout, in seconds, for a Places call.
```

## `schema_discovery.py slot-equivalence`

```text
usage: schema_discovery.py slot-equivalence [-h] [--registry REGISTRY] --out OUT --exceptions
       EXCEPTIONS --handoff-out HANDOFF_OUT --raw-dir RAW_DIR [--provider
       {anthropic,google,openai,openrouter}] [--enable] [--model MODEL] [--buddy | --no-buddy]
       [--buddy-provider {anthropic,google,openai,openrouter}] [--buddy-model BUDDY_MODEL]
       [--reasoning-effort REASONING_EFFORT] [--min-confidence MIN_CONFIDENCE] [--min-observations
       MIN_OBSERVATIONS] [--min-distinct-values MIN_DISTINCT_VALUES] consensus consensus_exceptions

positional arguments:
  consensus             consensus.py output whose fields are scanned for slot
                        collisions.
  consensus_exceptions  Matching consensus exception queue supplying each
                        engine's exact value per field.

options:
  -h, --help            show this help message and exit
  --registry REGISTRY   Approved registry of client-authorized rules. Only an
                        exact approved rule may map a source label.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --handoff-out HANDOFF_OUT
                        Destination path for the versioned, hash-bound handoff
                        this lane produces for downstream controls.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --provider {anthropic,google,openai,openrouter}
                        Provider serving this lane. Independence is resolved
                        to the model vendor behind any router.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --buddy, --no-buddy   Run the independent buddy pass (--no-buddy skips it).
                        This is an on/off switch, not a provider name -- the
                        buddy provider itself comes from --buddy-provider.
  --buddy-provider {anthropic,google,openai,openrouter}
                        Provider for the buddy lane. It must differ from the
                        primary provider.
  --buddy-model BUDDY_MODEL
                        Model identifier for the buddy lane.
  --reasoning-effort REASONING_EFFORT
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --min-confidence MIN_CONFIDENCE
                        Minimum proposal confidence retained. Client approval
                        is still required for every equivalence.
  --min-observations MIN_OBSERVATIONS
                        Shared-value observations a slot pair needs before it
                        is proposed at all.
  --min-distinct-values MIN_DISTINCT_VALUES
                        Distinct shared values a slot pair needs, which
                        separates a real equivalence from one repeated
                        coincidence.
```

## `schema_discovery.py registry-update`

```text
usage: schema_discovery.py registry-update [-h] --out OUT registry discovery decisions

positional arguments:
  registry    Existing approved semantic-mapping registry to amend.
  discovery   Retained discovery-proposal artifact this command reads.
  decisions   Client decision artifact naming which discovered mappings are
              approved.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `llm_adjudication.py`

```text
usage: llm_adjudication.py [-h] --out OUT --exceptions EXCEPTIONS --handoff-out HANDOFF_OUT --raw-
       dir RAW_DIR [--enable] [--model MODEL] [--reasoning-effort {none,low,medium,high,xhigh,max}]
       [--credential-env CREDENTIAL_ENV] [--min-confidence MIN_CONFIDENCE] [--sampling-rate
       SAMPLING_RATE] [--max-candidates MAX_CANDIDATES] [--max-pdf-bytes MAX_PDF_BYTES] [--timeout-
       seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--quiet] candidates manifest

Create audit-marked LLM amendment proposals without client approval.

positional arguments:
  candidates            explicit evidence-linked adjudication candidate JSON
  manifest              immutable intake ingestion_manifest.json

options:
  -h, --help            show this help message and exit
  --out OUT             new LLM adjudication artifact
  --exceptions EXCEPTIONS
                        new client-review exception artifact
  --handoff-out HANDOFF_OUT
                        new non-secret LLM handoff artifact
  --raw-dir RAW_DIR     new or empty retained raw-response directory
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --min-confidence MIN_CONFIDENCE
                        Minimum candidate confidence considered. Retained as
                        provenance; it never clears a control.
  --sampling-rate SAMPLING_RATE
                        Fraction of eligible candidates sampled.
  --max-candidates MAX_CANDIDATES
                        Maximum candidates this invocation may send.
  --max-pdf-bytes MAX_PDF_BYTES
                        Maximum bytes accepted for one retained page PDF. A
                        larger page is retained as an explicit exception
                        rather than truncated.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `completeness.py`

```text
usage: completeness.py [-h] [--gl GL] [--payments PAYMENTS] [--out OUT] [--gl-tolerance-pct
       GL_TOLERANCE_PCT] [--quiet] input

Produce the Completeness Report.

positional arguments:
  input                 proofed.json, consensus.json, or a record list

options:
  -h, --help            show this help message and exit
  --gl GL               GL export: CSV or JSON
  --payments PAYMENTS   Client payment or remittance export, CSV or JSON. A
                        row that cannot be keyed to an invoice is registered
                        as unusable, never dropped.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --gl-tolerance-pct GL_TOLERANCE_PCT
                        per-period variance tolerance (default 2%)
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `inferred_controls.py`

```text
usage: inferred_controls.py [-h] --out-dir OUT_DIR [--quiet] input

Build proposal-only inferred attribution, GL, and payment controls.

positional arguments:
  input              consensus/proofed JSON envelope

options:
  -h, --help         show this help message and exit
  --out-dir OUT_DIR  new empty output directory
  --quiet            suppress the summary
```

## `client_review_context.py`

```text
usage: client_review_context.py [-h] [--records RECORDS] [--max-documents MAX_DOCUMENTS] --out OUT
       responses

Build reasoning-only context from client responses.

positional arguments:
  responses             client_responses_preserved.json

options:
  -h, --help            show this help message and exit
  --records RECORDS     consensus/proofed JSON for a deterministic pilot
                        sample
  --max-documents MAX_DOCUMENTS
                        Maximum source documents this invocation may consider.
  --out OUT             new client_review_context_v1 JSON
```

## `client_input_comments.py`

```text
usage: client_input_comments.py [-h] --out OUT [--records RECORDS] [--max-documents MAX_DOCUMENTS]
       comments [comments ...]

Build hash-bound reasoning-only context from general client comments.

positional arguments:
  comments              Client comment files: JSON, CSV, TSV, TXT, Markdown,
                        or PDF. Repeatable.

options:
  -h, --help            show this help message and exit
  --out OUT             new client_review_context_v1 JSON
  --records RECORDS     Consensus or proofed JSON supplying the deterministic
                        pilot sample the relationship lanes reason over.
                        Without it they receive no records.
  --max-documents MAX_DOCUMENTS
                        Maximum source documents this invocation may consider.
```

## `client_review_inference.py`

```text
usage: client_review_inference.py [-h] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--workers WORKERS] [--provider {openai,google,openrouter,anthropic}] [--model
       MODEL] [--batch-size BATCH_SIZE] [--max-batches MAX_BATCHES] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--self-check-rounds
       SELF_CHECK_ROUNDS] context

Run bounded proposal-only LLM reference discovery with client context.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --workers WORKERS     batches discovered concurrently; output order is
                        unchanged by this setting
  --provider {openai,google,openrouter,anthropic}
                        Provider serving this lane. Independence is resolved
                        to the model vendor behind any router.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --batch-size BATCH_SIZE
                        Documents sent per request.
  --max-batches MAX_BATCHES
                        Maximum batches this invocation may send.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --self-check-rounds SELF_CHECK_ROUNDS
                        Same-model re-reading rounds. This improves a proposal
                        and is never independent consensus.
```

## `client_review_consensus.py`

```text
usage: client_review_consensus.py [-h] --primary PRIMARY --secondary-out SECONDARY_OUT
       --adjudication-out ADJUDICATION_OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR context

Run two Gemini proposal passes and OpenAI adjudication.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary PRIMARY     Retained primary-lane candidate artifact to replay
                        through this deprecated two-pass/adjudication flow.
                        Not a provider name -- providers are fixed to
                        google/openai.
  --secondary-out SECONDARY_OUT
                        Destination path for the second lane's retained
                        proposals.
  --adjudication-out ADJUDICATION_OUT
                        Destination path for the retained adjudication pass
                        over the two lanes.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
```

## `client_review_iterative.py`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py none`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py low`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py medium`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py high`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py xhigh`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_iterative.py max`

```text
usage: client_review_iterative.py [-h] --primary-out PRIMARY_OUT --buddy-out BUDDY_OUT --exceptions
       EXCEPTIONS --raw-dir RAW_DIR [--iterations ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--finalize-existing] [--reasoning-effort
       {none,low,medium,high,xhigh,max}] [--records RECORDS] context

Run configurable independent-primary/buddy iterative proposals.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --primary-out PRIMARY_OUT
                        Destination path for the primary lane's retained
                        relationship proposals.
  --buddy-out BUDDY_OUT
                        Destination path for the buddy lane's retained
                        confirmations.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --finalize-existing   write final artifacts from completed retained raw
                        packets without provider calls
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --records RECORDS     full consensus/proofed JSON corpus; overrides pilot
                        records
```

## `client_review_cross_packet.py`

```text
usage: client_review_cross_packet.py [-h] --records RECORDS --primary PRIMARY --buddy BUDDY --graph
       GRAPH --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR [--retry-exceptions
       RETRY_EXCEPTIONS] [--max-documents MAX_DOCUMENTS] [--max-neighborhoods MAX_NEIGHBORHOODS]
       [--max-verification-neighborhoods MAX_VERIFICATION_NEIGHBORHOODS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-iterations MAX_ITERATIONS] [--convergence-min-new-candidates
       CONVERGENCE_MIN_NEW_CANDIDATES] [--reasoning-effort {none,low,medium,high,xhigh,max}] context

Run bounded graph-guided cross-packet proposals and verification.

positional arguments:
  context               Hash-bound reasoning-only client context artifact.
                        Never source evidence, authorization, or independent
                        consensus input.

options:
  -h, --help            show this help message and exit
  --records RECORDS     Retained extracted-record artifact this command reads.
  --primary PRIMARY     Retained primary-lane relationship-proposal artifact
                        from client_review_iterative.py (--primary-out).
                        Provider selection for this lane comes only from
                        CLIENT_REVIEW_CROSS_PACKET_PRIMARY_PROVIDER in .env,
                        never from this option.
  --buddy BUDDY         Retained buddy-lane confirmation artifact from
                        client_review_iterative.py (--buddy-out). Provider
                        selection for this lane comes only from
                        CLIENT_REVIEW_CROSS_PACKET_BUDDY_PROVIDER in .env,
                        never from this option.
  --graph GRAPH         Retained deterministic evidence-graph artifact this
                        command reads.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --retry-exceptions RETRY_EXCEPTIONS
                        retry only provider-failed neighborhoods from this
                        retained exception artifact
  --max-documents MAX_DOCUMENTS
                        Maximum source documents this invocation may consider.
  --max-neighborhoods MAX_NEIGHBORHOODS
                        Maximum source neighborhoods inspected for new links
                        in one pass.
  --max-verification-neighborhoods MAX_VERIFICATION_NEIGHBORHOODS
                        Maximum neighborhoods used to independently verify
                        already-resolved proposals.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-iterations MAX_ITERATIONS
                        Maximum bounded passes this lane may run.
  --convergence-min-new-candidates CONVERGENCE_MIN_NEW_CANDIDATES
                        Stop after a completed pass yields fewer than this
                        many new source-backed, buddy-confirmed relationships.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `client_review_exception_resolution.py`

```text
usage: client_review_exception_resolution.py [-h] --exceptions EXCEPTIONS --out OUT --exceptions-out
       EXCEPTIONS_OUT --raw-dir RAW_DIR [--client-context CLIENT_CONTEXT] [--enable] [--workers
       WORKERS] records

Run bounded primary/buddy proposals for reassembly, validation, and
attribution exceptions.

positional arguments:
  records               Retained extracted-record artifact the supplied
                        exceptions refer to.

options:
  -h, --help            show this help message and exit
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions-out EXCEPTIONS_OUT
                        Destination path for the exception artifact naming
                        every input this command could not resolve.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --client-context CLIENT_CONTEXT
                        reasoning-only client_review_context_v1 artifact
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --workers WORKERS     batches resolved concurrently; output order is
                        unchanged by this setting
```

## `evidence_graph.py`

```text
usage: evidence_graph.py [-h] {manifest,build,query,path,candidates,contradictions,schema,schema-
       surface,schema-recovery-scope} ...

Build and query a deterministic JSON evidence graph.

positional arguments:
  {manifest,build,query,path,candidates,contradictions,schema,schema-surface,schema-recovery-scope}

options:
  -h, --help            show this help message and exit
```

## `evidence_graph.py manifest`

```text
usage: evidence_graph.py manifest [-h] --run-id RUN_ID --out OUT [--client-decisions PATH]
       [--client-review-cross-packet PATH] [--client-review-iterative-buddy PATH] [--client-review-
       iterative-primary PATH] [--consensus-records PATH] [--reference-adjudication PATH]
       [--reference-discovery PATH] [--validated-records PATH]

options:
  -h, --help            show this help message and exit
  --run-id RUN_ID       Identity recorded in the manifest and carried into the
                        graph.
  --out OUT             New manifest path. Every artifact must sit inside this
                        file's directory.
  --client-decisions PATH
                        Retained client decisions artifact. Repeatable.
  --client-review-cross-packet PATH
                        Retained client review cross packet artifact.
                        Repeatable.
  --client-review-iterative-buddy PATH
                        Retained client review iterative buddy artifact.
                        Repeatable.
  --client-review-iterative-primary PATH
                        Retained client review iterative primary artifact.
                        Repeatable.
  --consensus-records PATH
                        Retained consensus records artifact. Repeatable.
  --reference-adjudication PATH
                        Retained reference adjudication artifact. Repeatable.
  --reference-discovery PATH
                        Retained reference discovery artifact. Repeatable.
  --validated-records PATH
                        Retained validated records artifact. Repeatable.
```

## `evidence_graph.py build`

```text
usage: evidence_graph.py build [-h] --out OUT --exceptions EXCEPTIONS [--allow-empty-graph] manifest

positional arguments:
  manifest              Retained run manifest identifying every page artifact
                        and its hash.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --allow-empty-graph   retain a graph with no nodes instead of failing the
                        build
```

## `evidence_graph.py query`

```text
usage: evidence_graph.py query [-h] --out OUT [--node-type NODE_TYPE] [--property PROPERTY] [--limit
       LIMIT] graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --node-type NODE_TYPE
                        Restrict results to this node type.
  --property PROPERTY   Match a node property as NAME=VALUE. Repeatable; all
                        supplied properties must match.
  --limit LIMIT         Maximum number of results to return.
```

## `evidence_graph.py path`

```text
usage: evidence_graph.py path [-h] --out OUT --from SOURCE --to TARGET [--max-depth MAX_DEPTH] graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --from SOURCE         Node the path search starts from.
  --to TARGET           Node the path search must reach.
  --max-depth MAX_DEPTH
                        Maximum edges traversed before the search stops.
```

## `evidence_graph.py candidates`

```text
usage: evidence_graph.py candidates [-h] --out OUT --from SOURCE --relationship RELATIONSHIP
       [--limit LIMIT] graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --from SOURCE         Node whose candidate relationships are ranked.
  --relationship RELATIONSHIP
                        Relationship type to rank candidates for.
  --limit LIMIT         Maximum number of results to return.
```

## `evidence_graph.py contradictions`

```text
usage: evidence_graph.py contradictions [-h] --out OUT graph

positional arguments:
  graph       Retained evidence-graph artifact to read. The graph is never
              modified in place.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `evidence_graph.py schema`

```text
usage: evidence_graph.py schema [-h] --out OUT [--sample-limit SAMPLE_LIMIT] graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --sample-limit SAMPLE_LIMIT
                        Maximum observed sample values reported per claim.
```

## `evidence_graph.py schema-surface`

```text
usage: evidence_graph.py schema-surface [-h] --out OUT [--sample-limit SAMPLE_LIMIT] --consensus
       CONSENSUS graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --sample-limit SAMPLE_LIMIT
                        Maximum observed sample values reported per claim.
  --consensus CONSENSUS
                        Retained consensus JSON whose non-accepted candidate
                        values are inventoried.
```

## `evidence_graph.py schema-recovery-scope`

```text
usage: evidence_graph.py schema-recovery-scope [-h] --out OUT --consensus CONSENSUS [--topic TOPIC]
       [--signal-type SIGNAL_TYPE] graph

positional arguments:
  graph                 Retained evidence-graph artifact to read. The graph is
                        never modified in place.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --consensus CONSENSUS
                        Retained consensus artifact this command reads.
  --topic TOPIC         Restrict the recovery scope to this topic. Repeatable.
  --signal-type SIGNAL_TYPE
                        Restrict the recovery scope to this signal type.
                        Repeatable.
```

## `sampling.py`

```text
usage: sampling.py [-h] [--out OUT] --materiality MATERIALITY [--mus-n MUS_N] [--attribute-n
       ATTRIBUTE_N] [--tolerance TOLERANCE] [--findings FINDINGS] [--seed SEED] [--quiet] input

Build MUS and attribute samples; compute accuracy statement.

positional arguments:
  input                 proofed.json or a list of records

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --materiality MATERIALITY
                        documents at or above this are examined with certainty
  --mus-n MUS_N         MUS sample size below materiality (default 60)
  --attribute-n ATTRIBUTE_N
                        base attribute sample size before oversampling
                        (default 300)
  --tolerance TOLERANCE
                        tolerable misstatement in currency units; drives
                        escalation
  --findings FINDINGS   hash-bound sample_review_results_v1 JSON; switches to
                        projection mode
  --seed SEED           fixed for reproducibility -- a sample nobody can
                        reproduce is not auditable
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `analytics.py`

```text
usage: analytics.py [-h] [--review REVIEW] [--parties PARTIES] [--completeness COMPLETENESS] --out
       OUT input

Build a gate-aware analytics summary.

positional arguments:
  input                 validated/proofed/normalized JSON

options:
  -h, --help            show this help message and exit
  --review REVIEW       final_client_review.json
  --parties PARTIES     parties.json
  --completeness COMPLETENESS
                        completeness.json
  --out OUT             Destination path for this command's primary retained
                        artifact.
```

## `client_review_lane.py`

```text
usage: client_review_lane.py [-h] {build,read-answers,compile-inputs} ...

Build a client review pack from any blocked control, at any point in a run.
Point it at whatever exception or proposal artifacts a control produced. It
consolidates them into the exhaustive queue, groups repeated causes, reduces
those to the smallest set of questions that settles them, attaches the source
page behind each question, and writes a workbook to answer in and an
instruction PDF to answer from. The lane produces documents only when a
control clears its configured threshold, so it can be run after every stage
without generating a pack nobody needed. Use ``--force`` for a deliberate mid-
run client conversation; the pack records that it was forced. ``read-answers``
closes the loop: it validates a returned workbook against the immutable issued
copy and extracts the client's answers as decision proposals. Nothing is
applied by either verb.

positional arguments:
  {build,read-answers,compile-inputs}
    build               Build a review pack from blocked-control artifacts.
    read-answers        Extract a returned workbook's answers.
    compile-inputs      Build the two artifacts client_decision_compile.py
                        expects from a lane pack and its returned answers.
                        Without this a lane answer cannot reach an authorized
                        change.

options:
  -h, --help            show this help message and exit
```

## `client_review_lane.py build`

```text
usage: client_review_lane.py build [-h] --out-dir OUT_DIR [--consensus CONSENSUS] [--classifications
       CLASSIFICATIONS] [--resolved-by RESOLVED_BY] [--supersede ARTIFACT MANIFEST] [--settled-by-
       vote VOTE ARTIFACT] [--dispositions ARTIFACT] [--prefill-from PREFILL_FROM] [--scan-profile
       SCAN_PROFILE] [--manifest MANIFEST] [--max-questions MAX_QUESTIONS] [--render-dpi RENDER_DPI]
       [--max-image-bytes MAX_IMAGE_BYTES] [--force] [--quiet] artifacts [artifacts ...]

positional arguments:
  artifacts             Exception, proposal, or validation JSON artifacts from
                        any blocked control.

options:
  -h, --help            show this help message and exit
  --out-dir OUT_DIR     New directory for the pack; an existing directory is
                        refused.
  --consensus CONSENSUS
                        Consensus output used only to label document families;
                        groups are broader without it.
  --classifications CLASSIFICATIONS
                        Repeatable classification_consensus_v1 artifact. The
                        extraction consensus writes 'unknown' wherever its
                        lanes could not agree a document type; without this
                        the pack labels those documents unclassified even
                        where classification accepted a type.
  --resolved-by RESOLVED_BY
                        Repeatable accepted-classification artifact, as
                        final_review_queue.py takes. A document_type proposal
                        for a document that control already accepted, naming
                        the same type, is retained as reconciled instead of
                        being asked again.
  --supersede ARTIFACT MANIFEST
                        Repeatable, as final_review_queue.py takes it.
                        Findings in the input named ARTIFACT (by file name) on
                        the pages a subset re-read's MANIFEST lists are
                        retained as superseded rather than asked, because the
                        record carries the re-read's readings.
  --settled-by-vote VOTE ARTIFACT
                        Repeatable, as final_review_queue.py takes it.
                        Findings in the input named ARTIFACT (by file name) on
                        a field the multi_engine_vote.py artifact VOTE settled
                        are retained as settled_by_vendor_vote rather than
                        asked.
  --dispositions ARTIFACT
                        Repeatable, as final_review_queue.py takes it. An
                        operator-authorized operator_item_dispositions_v1
                        artifact; each item it names by review_item_id is
                        retained as dispositioned_by_operator rather than
                        asked.
  --prefill-from PREFILL_FROM
                        A prior round's client answer artifact. Matching
                        questions are pre-filled with what the client already
                        said, matched by group rather than question number, so
                        a rebuilt pack is a review of their answers rather
                        than a blank form to fill twice.
  --scan-profile SCAN_PROFILE
                        Scan profile from scan_profile.py. Pages it flags
                        near_blank are kept out of a question's worked
                        examples: a client shown a blank page and asked about
                        a layout answers about the blank page, and is right
                        to.
  --manifest MANIFEST   Intake manifest used to attach the source page behind
                        each question.
  --max-questions MAX_QUESTIONS
                        Largest number of questions to put to the client; the
                        rest are retained as deferred.
  --render-dpi RENDER_DPI
                        Resolution each example page is rendered at before
                        embedding.
  --max-image-bytes MAX_IMAGE_BYTES
                        Per-page render ceiling. An oversized render is an
                        exception, never a downscaled guess.
  --force               Build the pack even when no control cleared its
                        threshold; recorded in the pack.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `client_review_lane.py read-answers`

```text
usage: client_review_lane.py read-answers [-h] --issued-workbook ISSUED_WORKBOOK --pack PACK --out
       OUT [--quiet] workbook

positional arguments:
  workbook              Returned client workbook. Untrusted input.

options:
  -h, --help            show this help message and exit
  --issued-workbook ISSUED_WORKBOOK
                        The immutable workbook that was issued; the returned
                        copy is validated against it.
  --pack PACK           review_pack.json written beside the issued workbook.
  --out OUT             New client answer artifact; an existing path is
                        refused.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `client_review_lane.py compile-inputs`

```text
usage: client_review_lane.py compile-inputs [-h] --pack PACK --answers ANSWERS --issued-workbook
       ISSUED_WORKBOOK --returned-workbook RETURNED_WORKBOOK --consolidation-out CONSOLIDATION_OUT
       --decisions-out DECISIONS_OUT [--quiet]

options:
  -h, --help            show this help message and exit
  --pack PACK           review_pack.json from build.
  --answers ANSWERS     Client answer artifact from read-answers.
  --issued-workbook ISSUED_WORKBOOK
                        The immutable workbook that was issued.
  --returned-workbook RETURNED_WORKBOOK
                        The returned client workbook.
  --consolidation-out CONSOLIDATION_OUT
                        New consolidation-shaped artifact.
  --decisions-out DECISIONS_OUT
                        New workbook-import proposal.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `final_review_queue.py`

```text
usage: final_review_queue.py [-h] --out OUT [--resolved-by ARTIFACT] [--supersede ARTIFACT MANIFEST]
       [--settled-by-vote VOTE ARTIFACT] [--dispositions ARTIFACT] [--quiet] artifacts [artifacts
       ...]

Consolidate unresolved pipeline findings for final client review.

positional arguments:
  artifacts             exception/proposal/proof/validation JSON artifacts

options:
  -h, --help            show this help message and exit
  --out OUT             final client-review queue JSON
  --resolved-by ARTIFACT
                        Repeatable accepted-classification artifact. A raw
                        adapter `document_type` proposal for a document this
                        control already accepted, naming the same type, is
                        retained as reconciled instead of being asked again. A
                        proposal naming a different type stays blocking.
  --supersede ARTIFACT MANIFEST
                        Repeatable. Findings in the input named ARTIFACT (by
                        file name) on the pages a subset re-read's MANIFEST
                        lists are retained as superseded rather than queued,
                        because the record carries the re-read's readings.
                        Supply the re-read's own findings as inputs.
  --settled-by-vote VOTE ARTIFACT
                        Repeatable. Findings in the input named ARTIFACT (by
                        file name) on a field the multi_engine_vote.py
                        artifact VOTE settled are retained as
                        settled_by_vendor_vote rather than queued, because the
                        record carries the agreed value. Name the artifact
                        holding the open fields the vote was run on.
  --dispositions ARTIFACT
                        Repeatable. An operator-authorized
                        operator_item_dispositions_v1 artifact naming queue
                        items by review_item_id, each with the rule and
                        evidence that dispose of it. A named item is retained
                        as dispositioned_by_operator rather than queued. An
                        artifact that is not operator-authorized, or names no
                        item, is refused; a disposition that matches no item
                        is counted.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `client_review_llm.py`

```text
usage: client_review_llm.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir
       RAW_DIR [--enable] [--model MODEL] [--final-provider {openai,google,openrouter,anthropic}]
       [--final-model FINAL_MODEL] [--max-cards MAX_CARDS] [--max-context-bytes MAX_CONTEXT_BYTES]
       [--timeout-seconds TIMEOUT_SECONDS] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--auto-accept-llm-
       proposals] [--auto-accept-threshold AUTO_ACCEPT_THRESHOLD] [--final-run-include-protected-
       arithmetic-reassembly] [--reasoning-effort {none,low,medium,high,xhigh,max}] [--resume-from
       RESUME_FROM] [--workers WORKERS] queue

Run a bounded, proposal-only LLM client-review lane.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --max-cards MAX_CARDS
                        Maximum review cards this invocation may send.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
  --final-run-include-protected-arithmetic-reassembly
                        Allow source-backed proposals for protected
                        arithmetic/reassembly findings; they remain review-
                        required.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
  --resume-from RESUME_FROM
                        prior reviewer output; retry only unresolved cards
  --workers WORKERS     cards reviewed concurrently; output order is unchanged
                        by this setting
```

## `ai_simulated_client_review.py`

```text
usage: ai_simulated_client_review.py [-h] [--context CONTEXT] [--evidence EVIDENCE] --out OUT
       --exceptions-out EXCEPTIONS_OUT --raw-dir RAW_DIR --comments-csv COMMENTS_CSV --comments-xlsx
       COMMENTS_XLSX [--enable] [--workers WORKERS] [--resume] [--retry-exceptions RETRY_EXCEPTIONS]
       final_review

Create explicitly non-authoritative AI simulated-client review comments. The
command receives a final-review queue and optional hash-bound context, retains
one raw response per card, and writes a companion JSON/CSV/XLSX comment
package. It is decision support only: it cannot impersonate a client, apply a
decision, clear a protected finding, or permit canonical/CRM staging.

positional arguments:
  final_review          final_client_review.json input

options:
  -h, --help            show this help message and exit
  --context CONTEXT     optional reasoning-only JSON artifact
  --evidence EVIDENCE   source evidence JSON artifact; required for provider-
                        backed review
  --out OUT             new authoritative comment JSON
  --exceptions-out EXCEPTIONS_OUT
                        new explicit exception JSON
  --raw-dir RAW_DIR     new raw provider response directory
  --comments-csv COMMENTS_CSV
                        new companion CSV comment view
  --comments-xlsx COMMENTS_XLSX
                        new companion XLSX comment view
  --enable              explicitly allow provider calls
  --workers WORKERS     cards reviewed concurrently; output order is unchanged
                        by this setting
  --resume              resume an interrupted run from the hash-bound
                        checkpoint in --raw-dir
  --retry-exceptions RETRY_EXCEPTIONS
                        create a new no-clobber overlay for recoverable cards
                        in a prior exception artifact
```

## `client_review_cross_record.py`

```text
usage: client_review_cross_record.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--final-provider
       {openai,google,openrouter,anthropic}] [--model MODEL] [--timeout-seconds TIMEOUT_SECONDS]
       [--max-retries MAX_RETRIES] [--workers WORKERS] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-packet-bytes MAX_PACKET_BYTES] [--max-items-per-batch
       MAX_ITEMS_PER_BATCH] [--max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES] [--max-
       evidence-entries MAX_EVIDENCE_ENTRIES] [--auto-accept-llm-proposals] [--auto-accept-threshold
       AUTO_ACCEPT_THRESHOLD] queue

Run the optional cross-record client-review evidence pass.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated raw batches and retry only missing or
                        invalid batch files.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --workers WORKERS     batches searched concurrently; output order is
                        unchanged by this setting
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-packet-bytes MAX_PACKET_BYTES
                        Maximum UTF-8 bytes in one evidence packet.
  --max-items-per-batch MAX_ITEMS_PER_BATCH
                        Maximum review items sent in one batch.
  --max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES
                        maximum bytes for each evidence/producer/source
                        artifact before provider I/O
  --max-evidence-entries MAX_EVIDENCE_ENTRIES
                        maximum total declared entries across supplied
                        evidence envelopes
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
```

## `client_review_cross_record.py openai`

```text
usage: client_review_cross_record.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--final-provider
       {openai,google,openrouter,anthropic}] [--model MODEL] [--timeout-seconds TIMEOUT_SECONDS]
       [--max-retries MAX_RETRIES] [--workers WORKERS] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-packet-bytes MAX_PACKET_BYTES] [--max-items-per-batch
       MAX_ITEMS_PER_BATCH] [--max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES] [--max-
       evidence-entries MAX_EVIDENCE_ENTRIES] [--auto-accept-llm-proposals] [--auto-accept-threshold
       AUTO_ACCEPT_THRESHOLD] queue

Run the optional cross-record client-review evidence pass.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated raw batches and retry only missing or
                        invalid batch files.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --workers WORKERS     batches searched concurrently; output order is
                        unchanged by this setting
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-packet-bytes MAX_PACKET_BYTES
                        Maximum UTF-8 bytes in one evidence packet.
  --max-items-per-batch MAX_ITEMS_PER_BATCH
                        Maximum review items sent in one batch.
  --max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES
                        maximum bytes for each evidence/producer/source
                        artifact before provider I/O
  --max-evidence-entries MAX_EVIDENCE_ENTRIES
                        maximum total declared entries across supplied
                        evidence envelopes
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
```

## `client_review_cross_record.py google`

```text
usage: client_review_cross_record.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--final-provider
       {openai,google,openrouter,anthropic}] [--model MODEL] [--timeout-seconds TIMEOUT_SECONDS]
       [--max-retries MAX_RETRIES] [--workers WORKERS] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-packet-bytes MAX_PACKET_BYTES] [--max-items-per-batch
       MAX_ITEMS_PER_BATCH] [--max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES] [--max-
       evidence-entries MAX_EVIDENCE_ENTRIES] [--auto-accept-llm-proposals] [--auto-accept-threshold
       AUTO_ACCEPT_THRESHOLD] queue

Run the optional cross-record client-review evidence pass.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated raw batches and retry only missing or
                        invalid batch files.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --workers WORKERS     batches searched concurrently; output order is
                        unchanged by this setting
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-packet-bytes MAX_PACKET_BYTES
                        Maximum UTF-8 bytes in one evidence packet.
  --max-items-per-batch MAX_ITEMS_PER_BATCH
                        Maximum review items sent in one batch.
  --max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES
                        maximum bytes for each evidence/producer/source
                        artifact before provider I/O
  --max-evidence-entries MAX_EVIDENCE_ENTRIES
                        maximum total declared entries across supplied
                        evidence envelopes
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
```

## `client_review_cross_record.py openrouter`

```text
usage: client_review_cross_record.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--final-provider
       {openai,google,openrouter,anthropic}] [--model MODEL] [--timeout-seconds TIMEOUT_SECONDS]
       [--max-retries MAX_RETRIES] [--workers WORKERS] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-packet-bytes MAX_PACKET_BYTES] [--max-items-per-batch
       MAX_ITEMS_PER_BATCH] [--max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES] [--max-
       evidence-entries MAX_EVIDENCE_ENTRIES] [--auto-accept-llm-proposals] [--auto-accept-threshold
       AUTO_ACCEPT_THRESHOLD] queue

Run the optional cross-record client-review evidence pass.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated raw batches and retry only missing or
                        invalid batch files.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --workers WORKERS     batches searched concurrently; output order is
                        unchanged by this setting
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-packet-bytes MAX_PACKET_BYTES
                        Maximum UTF-8 bytes in one evidence packet.
  --max-items-per-batch MAX_ITEMS_PER_BATCH
                        Maximum review items sent in one batch.
  --max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES
                        maximum bytes for each evidence/producer/source
                        artifact before provider I/O
  --max-evidence-entries MAX_EVIDENCE_ENTRIES
                        maximum total declared entries across supplied
                        evidence envelopes
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
```

## `client_review_cross_record.py anthropic`

```text
usage: client_review_cross_record.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS
       --raw-dir RAW_DIR [--resume-raw-dir RESUME_RAW_DIR] [--enable] [--final-provider
       {openai,google,openrouter,anthropic}] [--model MODEL] [--timeout-seconds TIMEOUT_SECONDS]
       [--max-retries MAX_RETRIES] [--workers WORKERS] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--max-context-bytes
       MAX_CONTEXT_BYTES] [--max-packet-bytes MAX_PACKET_BYTES] [--max-items-per-batch
       MAX_ITEMS_PER_BATCH] [--max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES] [--max-
       evidence-entries MAX_EVIDENCE_ENTRIES] [--auto-accept-llm-proposals] [--auto-accept-threshold
       AUTO_ACCEPT_THRESHOLD] queue

Run the optional cross-record client-review evidence pass.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --resume-raw-dir RESUME_RAW_DIR
                        Reuse validated raw batches and retry only missing or
                        invalid batch files.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --workers WORKERS     batches searched concurrently; output order is
                        unchanged by this setting
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-packet-bytes MAX_PACKET_BYTES
                        Maximum UTF-8 bytes in one evidence packet.
  --max-items-per-batch MAX_ITEMS_PER_BATCH
                        Maximum review items sent in one batch.
  --max-evidence-artifact-bytes MAX_EVIDENCE_ARTIFACT_BYTES
                        maximum bytes for each evidence/producer/source
                        artifact before provider I/O
  --max-evidence-entries MAX_EVIDENCE_ENTRIES
                        maximum total declared entries across supplied
                        evidence envelopes
  --auto-accept-llm-proposals
                        Allow proposals at or above the threshold to be marked
                        auto-accepted. Protected findings are never eligible.
  --auto-accept-threshold AUTO_ACCEPT_THRESHOLD
                        Proposal score at or above which auto-acceptance may
                        apply. Provider confidence is not a decision term.
```

## `review_agent.py`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py none`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py low`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py medium`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py high`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py xhigh`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_agent.py max`

```text
usage: review_agent.py [-h] [--context CONTEXT] --out OUT --exceptions EXCEPTIONS --raw-dir RAW_DIR
       [--enable] [--final-provider {openai,google,openrouter,anthropic}] [--final-model
       FINAL_MODEL] [--model MODEL] [--iterations ITERATIONS] [--workers WORKERS] [--max-context-
       bytes MAX_CONTEXT_BYTES] [--max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES] [--max-evidence-
       per-slice MAX_EVIDENCE_PER_SLICE] [--max-retries MAX_RETRIES] [--retry-backoff-seconds
       RETRY_BACKOFF_SECONDS] [--max-backoff-seconds MAX_BACKOFF_SECONDS] [--reasoning-effort
       {none,low,medium,high,xhigh,max}]

Run the proposal-only full-dataset review agent.

options:
  -h, --help            show this help message and exit
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --exceptions EXCEPTIONS
                        Destination path for the exception artifact naming
                        every input this command could not resolve. Absence of
                        exceptions is a result, not a formality.
  --raw-dir RAW_DIR     Directory retaining the raw provider response for
                        every request, as provenance. Raw responses stay with
                        the operator and never enter a client package.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --final-provider {openai,google,openrouter,anthropic}
                        Provider serving this lane's final pass.
  --final-model FINAL_MODEL
                        Model identifier for this lane's final pass.
  --model MODEL         Model identifier for the configured provider. Retained
                        as run provenance.
  --iterations ITERATIONS
                        Maximum bounded passes this lane may run.
  --workers WORKERS     slices analysed concurrently; output order is
                        unchanged by this setting
  --max-context-bytes MAX_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one request packet.
                        Work that cannot fit is retained as an explicit
                        exception.
  --max-slice-context-bytes MAX_SLICE_CONTEXT_BYTES
                        Maximum UTF-8 bytes of context in one analysis slice.
  --max-evidence-per-slice MAX_EVIDENCE_PER_SLICE
                        Maximum evidence entries included in one analysis
                        slice.
  --max-retries MAX_RETRIES
                        Maximum bounded retries per request after a transient
                        provider failure.
  --retry-backoff-seconds RETRY_BACKOFF_SECONDS
                        Initial delay, in seconds, between bounded retries.
  --max-backoff-seconds MAX_BACKOFF_SECONDS
                        Upper bound, in seconds, on retry backoff growth.
  --reasoning-effort {none,low,medium,high,xhigh,max}
                        Reasoning effort requested from the model. Retained as
                        provenance; never a decision term.
```

## `review_grouping.py`

```text
usage: review_grouping.py [-h] --consensus CONSENSUS [--classifications ARTIFACT] [--out OUT] queue

Build proposal-only root-cause groups for a client review queue. The
exhaustive queue remains the source of truth. This command creates a smaller
decision surface by grouping repeated template, provider-disagreement, and
wrapper findings. A group is a proposed batch rule, never an approval or
deletion of its underlying evidence.

positional arguments:
  queue                 Retained final-review queue artifact.

options:
  -h, --help            show this help message and exit
  --consensus CONSENSUS
                        Retained consensus artifact this command reads.
  --classifications ARTIFACT
                        Repeatable classification_consensus_v1 artifact. The
                        extraction consensus writes 'unknown' where its lanes
                        could not agree a document type, which is most of a
                        corpus this lane was built for; without this the
                        groups are labelled unclassified_document_family even
                        where classification accepted a type.
  --out OUT             Destination path for this command's primary retained
                        artifact.
```

## `safe_review_consolidation.py`

```text
usage: safe_review_consolidation.py [-h] --run-dir RUN_DIR [--output-dir OUTPUT_DIR] [--queue QUEUE]
       [--client-review CLIENT_REVIEW] [--context CONTEXT] [--out OUT] [--enable] [--carry-forward]
       [--carry-forward-threshold CARRY_FORWARD_THRESHOLD] [--grouping-consensus GROUPING_CONSENSUS]
       [--disable-grouping] [--disable-client-package]

Run and safely consolidate the post-client-review proposal lanes. This command
reuses the existing cross-record and full-dataset agents, then builds a
smaller client-facing view without deleting source review items. Protected
findings never qualify for carry-forward. The output is a review artifact, not
an approved-facts artifact.

options:
  -h, --help            show this help message and exit
  --run-dir RUN_DIR     Run directory holding the artifacts this orchestration
                        reads.
  --output-dir OUTPUT_DIR
                        Directory, relative to the run directory, for the
                        consolidation artifacts.
  --queue QUEUE         Final-review queue filename within the run directory.
  --client-review CLIENT_REVIEW
                        Card-review artifact filename within the run
                        directory.
  --context CONTEXT     Hash-bound reasoning-only client context. It may
                        explain terminology or priorities and is never source
                        evidence, authorization, or independent consensus
                        input.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
  --carry-forward       Allow eligible proposals at or above the threshold to
                        carry forward. Protected categories never carry
                        forward.
  --carry-forward-threshold CARRY_FORWARD_THRESHOLD
                        Score at or above which an eligible proposal may carry
                        forward.
  --grouping-consensus GROUPING_CONSENSUS
                        Optional consensus artifact used for proposal-only
                        document-family grouping.
  --disable-grouping    Skip root-cause grouping. Every underlying queue item
                        is retained either way.
  --disable-client-package
                        Do not create the small client workbook/README package
                        after consolidation.
```

## `client_review_package.py`

```text
usage: client_review_package.py [-h] {create,import-decisions,preserve-responses} ...

Create the small client-facing package from safe consolidation JSON. The
consolidation JSON remains the internal audit artifact. This package is
deliberately limited to an executive summary, group-level decisions, and one
representative card per decision group. It does not export the exhaustive
queue, a drill-down, or an appendix.

positional arguments:
  {create,import-decisions,preserve-responses}
    preserve-responses  snapshot exact client choices and notes without
                        applying them

options:
  -h, --help            show this help message and exit
```

## `client_review_package.py create`

```text
usage: client_review_package.py create [-h] --out-dir OUT_DIR consolidation

positional arguments:
  consolidation      Safe-consolidation artifact the issued workbook is built
                     from.

options:
  -h, --help         show this help message and exit
  --out-dir OUT_DIR  New directory for this command's retained artifacts. An
                     existing directory is refused.
```

## `client_review_package.py import-decisions`

```text
usage: client_review_package.py import-decisions [-h] --issued-workbook ISSUED_WORKBOOK --out OUT
       workbook

positional arguments:
  workbook              Returned client workbook. Untrusted input, validated
                        against the issued copy before it is read.

options:
  -h, --help            show this help message and exit
  --issued-workbook ISSUED_WORKBOOK
                        The exact workbook that was issued. The return is
                        validated against it.
  --out OUT             Destination path for this command's primary retained
                        artifact.
```

## `client_review_package.py preserve-responses`

```text
usage: client_review_package.py preserve-responses [-h] [--issued-workbook ISSUED_WORKBOOK] --out
       OUT workbook

positional arguments:
  workbook              Returned client workbook whose editable responses are
                        preserved unchanged.

options:
  -h, --help            show this help message and exit
  --issued-workbook ISSUED_WORKBOOK
                        The exact workbook that was issued. The return is
                        validated against it.
  --out OUT             Destination path for this command's primary retained
                        artifact.
```

## `simulate_client_approval.py`

```text
usage: simulate_client_approval.py [-h] --out OUT [--downstream-gate-out DOWNSTREAM_GATE_OUT] queue

Create a repeatable, simulation-only approval projection. This command models
a client approving every supplied review item. It changes disposition in a
separate artifact only; it never changes source evidence, canonical review
facts, or production retrieval eligibility.

positional arguments:
  queue                 canonical final_client_review.json

options:
  -h, --help            show this help message and exit
  --out OUT             new simulation JSON output
  --downstream-gate-out DOWNSTREAM_GATE_OUT
                        optional terminal simulation gate output
```

## `golden_set_evaluate.py`

```text
usage: golden_set_evaluate.py [-h] --out OUT --exceptions EXCEPTIONS [--minimum-precision
       MINIMUM_PRECISION] [--minimum-recall MINIMUM_RECALL] [--minimum-coverage MINIMUM_COVERAGE]
       [--max-false-auto-accepts MAX_FALSE_AUTO_ACCEPTS] [--max-protected-escapes
       MAX_PROTECTED_ESCAPES] truth predictions

Evaluate a full pipeline result against client-authorized golden truth.

positional arguments:
  truth                 client-authorized golden truth JSON
  predictions           pipeline prediction JSON

options:
  -h, --help            show this help message and exit
  --out OUT             new evaluation report JSON
  --exceptions EXCEPTIONS
                        new review artifact JSON
  --minimum-precision MINIMUM_PRECISION
                        required exact-value precision
  --minimum-recall MINIMUM_RECALL
                        required exact-value recall
  --minimum-coverage MINIMUM_COVERAGE
                        required usable-prediction rate
  --max-false-auto-accepts MAX_FALSE_AUTO_ACCEPTS
                        maximum wrong auto-accepted values
  --max-protected-escapes MAX_PROTECTED_ESCAPES
                        maximum protected auto-acceptances
```

## `field_extract_export.py`

```text
usage: field_extract_export.py [-h] --manifest MANIFEST [--final-review FINAL_REVIEW] --out OUT
       [--csv CSV] [--accepted-csv ACCEPTED_CSV] [--lines-csv LINES_CSV] [--documents-csv
       DOCUMENTS_CSV] [--items-csv ITEMS_CSV] [--parties PARTIES] [--two-digit-year-months
       AUTHORIZATION] [--rows-not-printed PROPOSALS] [--rows-not-printed-authorization
       AUTHORIZATION] [--quiet] records

Export every extracted field with the status that governs it.
`canonical_export.py` admits only review-clear documents and has no override.
That rule is correct and this command does not touch it -- but on a corpus
where every document carries an open exception it means nothing reaches a file
at all. On the commission run that is 0 of 716 documents, while 73,878 of the
83,453 extracted fields are already accepted. Those readings exist, they are
provenance-linked, and there was no way to see them outside the artifacts.
**Export the run's current records artifact, not its consensus artifact.** A
consensus artifact is where acceptance *starts*, not where it ends.
`apply_corroboration.py` and `apply_mappings.py` both append acceptances after
it, and on that run the difference was 26,300 accepted fields against 73,878
-- the same 716 documents and the same 83,453 fields, read one step too early.
The consensus artifact answers "what did two vendors agree", which is a real
question and not the one an export asks. Point this at the newest
`records_with_applied_mappings_v1`, or at whatever artifact the run's own lane
coverage names last; `summary.source_artifact` on each record artifact names
the one it was built from, so the chain can be walked forward. This command
writes all of them, and everything else beside them. Every field of every
document appears exactly once, carrying the evidence that accepted it or the
reason it is withheld. Nothing is dropped and nothing is promoted: a withheld
row is a retained reading, not an approved fact, and it is labelled as one on
the row rather than in a footnote. A status per kind of evidence, because
"accepted" hides the distinction that decides what an operator may do with a
row: * `accepted_vendor_agreement` -- two independent model vendors read the
same value. This is the only status the canonical load would consider. *
`accepted_independent_corroboration` -- one model read it and an independent
non-LLM extractor read the same value on the same page. That defeats a
misread, not a misplacement, so it is not vendor agreement. *
`accepted_same_document` -- the value matches one already settled elsewhere in
the same document. * `accepted_extractor_tiebreak` -- vendors split and the
non-LLM extractor matched one side. * `accepted_page_review_amendment` -- an
authorized amendment from reading the page against its image, made only with
evidence independent of the reviewer: the non-LLM extractor's text of the page
or a retained engine reading. The value it replaced stays on the field. *
`accepted_approved_mapping` -- two engines read the value under a printed
label an approved rule maps to this field. The rule places it; no vendor was
asked which field it belongs in. * `accepted_document_arithmetic` -- the
document's own arithmetic identifies the reading: amount, base and rate must
agree, and only one candidate makes them. * `accepted_derived` -- a structural
value the pipeline computed, such as a line ordinal. Accepted, but it is not a
reading of the page and no vendor attested it. * `single_reading` -- exactly
one engine returned a value. Not a disagreement: there is no second reading to
disagree with. Treating it as contested asks a question with no answer, and
treating it as agreed claims corroboration that does not exist. * `contested`
-- engines returned different values. This is a real dispute and the
candidates are carried on the row. Every row also carries `confidence`: one
word -- `confirmed`, `corroborated`, `computed`, `derived` or `unconfirmed` --
for a reader who needs to act on the cell rather than audit it. A pivoted CRM
row carries `row_confidence` and `unconfirmed_fields`, because a row is only
as trustworthy as its weakest cell and an importer showing a row-level mark
needs the names behind it. This is not a canonical export and it clears no
gate. `crm_import_package.py`, `crm_export_jobs.py` and the rest of the load
chain read `canonical_export_v1` and are unaffected by this artifact; nothing
here becomes loadable by being written down. What it does is make the run's
own evidence readable outside the pipeline, per field, with the provenance
needed to decide about it.

positional arguments:
  records               The run's current retained record artifact -- normally
                        the newest records_with_applied_mappings_v1. A
                        consensus artifact is accepted and is usually the
                        wrong input: acceptances are appended after it.

options:
  -h, --help            show this help message and exit
  --manifest MANIFEST   intake ingestion_manifest.json
  --final-review FINAL_REVIEW
                        Final review queue JSON. Without it the
                        blocking_review_reasons column is empty for every row,
                        which is indistinguishable from a corpus with no
                        findings; the summary reports whether it was supplied.
  --out OUT             New field-extract artifact path.
  --csv CSV             Also write every row as CSV at this path.
  --accepted-csv ACCEPTED_CSV
                        Also write only the accepted rows as CSV: every row
                        whose status is one of accepted_vendor_agreement,
                        accepted_independent_corroboration,
                        accepted_same_document, accepted_extractor_tiebreak,
                        accepted_page_review_amendment,
                        accepted_approved_mapping,
                        accepted_document_arithmetic, accepted_derived. Only
                        accepted_vendor_agreement means two independent
                        vendors read the same value; each other status names
                        the different evidence that accepted its row, and a
                        row accepted by an unregistered lane is left out. It
                        is still not a canonical load.
  --lines-csv LINES_CSV
                        Also write one row per commission line, pivoted back
                        into the record grain a CRM import loads. Each row
                        carries how many of its cells two vendors agreed, how
                        many rest on one engine, and which are contested, by
                        name.
  --documents-csv DOCUMENTS_CSV
                        Also write one row per document from its header
                        fields, with the same evidence profile.
  --items-csv ITEMS_CSV
                        Also write the distinct products the line grain names,
                        one row each. Loading the line grain as products
                        creates one product per line; this collapses the
                        repeats and labels a reading whose shape is not a
                        product.
  --parties PARTIES     Party master from entity_resolve.py. Without it the
                        resolved_party column is empty on every row, which
                        reads as a corpus whose names resolve to nothing; the
                        summary reports whether it was supplied.
  --two-digit-year-months AUTHORIZATION
                        Type a month printed with a two-digit year
                        (`December-24`, Excel's mmm-yy) as that month of 20YY
                        in <column>__month, under the named operator
                        authorization. Without it such a cell stays untyped,
                        because nothing in the cell says whether 24 is a year
                        or a day.
  --rows-not-printed PROPOSALS
                        Proposals from page_review.py --proposals. Each line
                        the page review says the page does not print is
                        labelled in page_review_row_not_printed with its
                        evidence -- the extractor prints its figure fewer
                        times, the row carries no money, or a second vendor's
                        reading holds every other figure and this one fewer
                        times -- and is never dropped. A line no longer
                        holding the figure the review named is counted and
                        left unlabelled. Needs --lines-csv and --rows-not-
                        printed-authorization.
  --rows-not-printed-authorization AUTHORIZATION
                        The operator authorization the row labels are carried
                        under.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `classification_amend.py`

```text
usage: classification_amend.py [-h] --out OUT [--exceptions EXCEPTIONS] [--exceptions-out
       EXCEPTIONS_OUT] [--records ARTIFACT] [--outlier-factor OUTLIER_FACTOR] [--supersede-prior-
       client-decisions] [--quiet] classification authorization

Carry an operator-authorized client decision into document classification.
`classification_consensus.py` accepts a document type only where two
independent model vendors already agreed. That is the right rule, and it
leaves a gap: a corpus where the vendors could not agree keeps
`document_family: unknown`, every later lane inherits it, and the client is
asked to confirm the type. When the client answers, nothing carries the answer
back. One run made that concrete. 166 of 716 documents were unresolved; the
client confirmed the type for 136 of them; the answer was compiled,
authorized, and written into the approved template registry -- and not one
document's classification changed, because no control reads that registry. The
approval was real and inert. This command closes that one step. It reads an
operator-authorized compiled plan and appends a classification for each
document its authorized patches name. Two change types reach it, because the
review lane compiles two different questions: a client approving a layout
produces `template_registry_rule`, which carries the family on its approved
template, and a client confirming what a document *is* produces
`document_type_rule`, which carries the family as the proposed value and has
no layout fingerprint at all. Both name a document family, so both apply here;
an authorization holding neither is refused by name. Three refusals keep it
from becoming an override channel: * an authorization that is not
`operator_authorized` is refused, so a client note alone can never reach
classification; * a document accepted on independent vendor agreement is
refused rather than replaced -- vendor agreement is evidence, and a client
assertion does not outrank it; * a document whose type came from an *earlier
client decision* is a different case, and was being refused with the same
message. Nothing about it is vendor agreement: the same authority is
correcting itself, which is the normal correction path and the reason
amendments are append-only. It is still not something to do by accident, so it
needs `--supersede-prior-client-decisions`; the refusal without that flag
reports the whole set and the retyping it would perform, so an operator
decides once rather than one document at a time; * a document outside the
authorized patches' own impact lists is refused, so the blast radius is
exactly what the operator authorized. Amended entries carry `evidence:
operator_authorized_client_decision` and the authorization id, so they never
read as vendor agreement. The input artifact is not modified; this writes a
new one. Appending an acceptance is only half the job. The classification
exception that sent the document to the client is still standing, and
`final_review_queue.py` builds the gate from those exception artifacts -- so
without the second half the client is asked the same question again after
answering it. Pass `--exceptions` and `--exceptions-out` to mark each resolved
entry `client_review_required: false`, which is the mechanism the gate already
honours. Nothing is deleted: the exception stays in the artifact, carrying the
authorization that resolved it.

positional arguments:
  classification        Retained classification-consensus artifact.
  authorization         Operator-authorized compiled plan JSON.

options:
  -h, --help            show this help message and exit
  --out OUT             New classification artifact path.
  --exceptions EXCEPTIONS
                        Retained classification-exception artifact. Without it
                        the exceptions that sent these documents to the client
                        stay open and the gate asks again.
  --exceptions-out EXCEPTIONS_OUT
                        New exception artifact path.
  --records ARTIFACT    Repeatable retained extraction artifact. A client
                        answers one question for a whole group; a member
                        carrying far more extracted content than the group's
                        median is a member that answer was visibly not about,
                        and is withheld from the amendment rather than typed
                        from it. Without this the check does not run and the
                        summary says so instead of reporting zero.
  --outlier-factor OUTLIER_FACTOR
                        How many times its group's median populated-field
                        count a document may carry before its group's answer
                        is treated as not covering it.
  --supersede-prior-client-decisions
                        Let this authorization retype a document an earlier
                        client decision typed. The earlier acceptance is
                        retained and both authorizations are linked; vendor
                        agreement is never superseded, with or without this.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `client_decision_compile.py`

```text
usage: client_decision_compile.py [-h] {compile,authorize} ...

Compile returned client decisions into an operator-authorized change plan. Six
change types compile and authorize, but only ``template_registry_rule`` has an
implemented consumer: ``template_drift.py registry-update``. A plan built from
``mapping_rule``, ``document_type_rule``, ``amendment``,
``allocation_policy_rule`` or ``source_quality_action`` compiles cleanly,
binds a real operator signature, and then fails at apply with "authorization
contains no template_registry_rule patches" -- after someone has signed.
Choose the change type against its consumer before compiling. An operator
correction to a client's wording goes in ``proposed_value``. This command
rejects a catalog whose ``decision_comment`` differs from the client's note,
so the returned text stays byte-exact through the chain. ``authorize`` binds
``compiled_plan_sha256``. Re-cutting the catalog produces a different plan and
needs a fresh signature; the previous one does not carry over.

positional arguments:
  {compile,authorize}
    compile            build an impact preview and default-defer template
    authorize          bind operator choices to an unchanged plan

options:
  -h, --help           show this help message and exit
```

## `client_decision_compile.py compile`

```text
usage: client_decision_compile.py compile [-h] --out OUT --authorization-template
       AUTHORIZATION_TEMPLATE decisions consolidation catalog

positional arguments:
  decisions             complete workbook-import proposal JSON
  consolidation         safe consolidation JSON
  catalog               explicit append-only change catalog JSON

options:
  -h, --help            show this help message and exit
  --out OUT             new compiled plan JSON
  --authorization-template AUTHORIZATION_TEMPLATE
                        new default-defer authorization JSON
```

## `client_decision_compile.py authorize`

```text
usage: client_decision_compile.py authorize [-h] --out OUT plan decisions

positional arguments:
  plan        compiled plan JSON
  decisions   completed operator authorization JSON

options:
  -h, --help  show this help message and exit
  --out OUT   new authorized plan JSON
```

## `operations.py`

```text
usage: operations.py [-h] {manifest,stage,adapter,privacy,review-export} ...

Operational safeguards for auditable ingestion runs.

positional arguments:
  {manifest,stage,adapter,privacy,review-export}

options:
  -h, --help            show this help message and exit
```

## `operations.py manifest`

```text
usage: operations.py manifest [-h] [--config CONFIG] [--config-from-env] --out OUT artifacts
       [artifacts ...]

positional arguments:
  artifacts          Retained artifacts to hash into the manifest.

options:
  -h, --help         show this help message and exit
  --config CONFIG    Inline JSON run configuration recorded in the manifest.
  --config-from-env  merge the non-secret effective .env/process settings into
                     the manifest
  --out OUT          Destination path for this command's primary retained
                     artifact.
```

## `operations.py stage`

```text
usage: operations.py stage [-h] --manifest-hash MANIFEST_HASH state
       {profile,intake,extract,validate,review}

positional arguments:
  state                 Stage-state artifact to update.
  {profile,intake,extract,validate,review}
                        Pipeline stage this state transition records.

options:
  -h, --help            show this help message and exit
  --manifest-hash MANIFEST_HASH
                        Hash of the manifest this stage state is bound to. A
                        mismatch is refused.
```

## `operations.py adapter`

```text
usage: operations.py adapter [-h] --type
       {ocr,htr,llm_adjudication,schema_discovery,allocation_policy,table_comprehension}
       --credential-env CREDENTIAL_ENV --out OUT input

positional arguments:
  input                 Provider handoff the adapter contract is built from.

options:
  -h, --help            show this help message and exit
  --type {ocr,htr,llm_adjudication,schema_discovery,allocation_policy,table_comprehension}
                        Adapter contract type to emit.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --out OUT             Destination path for this command's primary retained
                        artifact.
```

## `operations.py privacy`

```text
usage: operations.py privacy [-h] --out OUT texts [texts ...]

positional arguments:
  texts       Text artifacts scanned for a privacy inventory.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `operations.py review-export`

```text
usage: operations.py review-export [-h] --csv CSV --html HTML --xlsx XLSX input

positional arguments:
  input        Review artifact exported for reviewers.

options:
  -h, --help   show this help message and exit
  --csv CSV    Destination path for the CSV reviewer export.
  --html HTML  Destination path for the HTML reviewer export.
  --xlsx XLSX  Destination path for the XLSX reviewer export.
```

## `run_workspace.py`

```text
usage: run_workspace.py [-h] {init,run,audit} ...

Create, execute, and audit one contained business-document pipeline run.
Operational artifacts are evidence. A prior trial split its output between a
named run directory, later sibling phase directories, and repository-root LLM
cache/throttle directories. That makes a run impossible to review as one
bounded record. This command establishes a fresh run workspace, launches
existing pipeline CLIs with that workspace as their working directory, and
rejects declared output paths that leave it. Immutable source inputs and
credentials remain outside the workspace. Every generated artifact --
including caches, throttles, logs, raw responses, and retry overlays --
belongs below it.

positional arguments:
  {init,run,audit}
    init            Create a fresh standard run workspace.
    run             Run one existing pipeline command inside a workspace.
    audit           Audit workspace containment and command ledger.

options:
  -h, --help        show this help message and exit
```

## `run_workspace.py init`

```text
usage: run_workspace.py init [-h] [--out OUT] run_root

positional arguments:
  run_root    New or empty root directory for one run.

options:
  -h, --help  show this help message and exit
  --out OUT   New initialization record under the run root.
```

## `run_workspace.py run`

```text
usage: run_workspace.py run [-h] run_root ...

positional arguments:
  run_root    Existing run workspace root.
  command     Command after -- to execute.

options:
  -h, --help  show this help message and exit
```

## `run_workspace.py audit`

```text
usage: run_workspace.py audit [-h] [--out OUT] run_root

positional arguments:
  run_root    Existing run workspace root.

options:
  -h, --help  show this help message and exit
  --out OUT   New containment-audit JSON path under the run root; defaults to
              logs/run_workspace_audit.json.
```

## `visual_ingestion_export.py`

```text
usage: visual_ingestion_export.py [-h] {export,verify} ...

Preserve a visual-intake session for a new run; never promote model readings.

positional arguments:
  {export,verify}
    export         retain one session and prepare source-only PDF
    verify         verify a package against the retained manifest checksum

options:
  -h, --help       show this help message and exit
```

## `visual_ingestion_export.py export`

```text
usage: visual_ingestion_export.py export [-h] --tenant TENANT --owner OWNER --session-id SESSION_ID
       --out-dir OUT_DIR directory

positional arguments:
  directory             existing private intake directory, opened read-only

options:
  -h, --help            show this help message and exit
  --tenant TENANT       exact tenant bound to the journal
  --owner OWNER         exact session owner; trusted local operator only
  --session-id SESSION_ID
                        session ID from the intake receipt
  --out-dir OUT_DIR     new private source-package directory inside the run
```

## `visual_ingestion_export.py verify`

```text
usage: visual_ingestion_export.py verify [-h] --manifest-sha256 MANIFEST_SHA256 directory

positional arguments:
  directory             retained source-package directory; no writes

options:
  -h, --help            show this help message and exit
  --manifest-sha256 MANIFEST_SHA256
                        exact export receipt checksum, retained separately
```

## `llm_usage_report.py`

```text
usage: llm_usage_report.py [-h] --out OUT run_dir

Capture retained LLM usage metadata for a run.

positional arguments:
  run_dir     Run directory whose retained artifacts are summarized. No
              provider is contacted.

options:
  -h, --help  show this help message and exit
  --out OUT   Destination path for this command's primary retained artifact.
```

## `run_monitor.py`

```text
usage: run_monitor.py [-h] [--expected-pages EXPECTED_PAGES] [--interval INTERVAL] [--out OUT]
       [--price-file PRICE_FILE] [--required-service REQUIRED_SERVICE] [--once] [--stop-when-
       complete] run_dir

Monitor run progress and estimated spend from retained run artifacts. The
monitor is intentionally read-only with respect to provider services. It polls
a run directory, counts retained responses and service summaries, and writes
an atomic JSON snapshot. Credentials and response content are never printed or
copied into the snapshot. Examples -------- One snapshot:: python
scripts/run_monitor.py RUN_DIR --expected-pages 51 --once Live terminal
monitor:: python scripts/run_monitor.py RUN_DIR --expected-pages 51 --interval
2 --out RUN_DIR/progress_monitor.json --stop-when-complete Pricing is an
estimate. Override the built-in rates with ``--price-file`` when the billing
account, SKU, or provider contract differs.

positional arguments:
  run_dir               Run directory whose retained progress is monitored.

options:
  -h, --help            show this help message and exit
  --expected-pages EXPECTED_PAGES
                        Expected page count, used to report completion
                        percentage.
  --interval INTERVAL   Seconds between snapshots.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --price-file PRICE_FILE
                        JSON price table used to estimate spend. Estimates are
                        indicative, not billing.
  --required-service REQUIRED_SERVICE
                        Service that must appear before the run counts as
                        complete. Repeatable.
  --once                Take a single snapshot and exit instead of monitoring
                        continuously.
  --stop-when-complete  Exit once every required service has reported and the
                        expected pages are accounted for.
```

## `reauthorize_google.py`

```text
usage: reauthorize_google.py [-h] [--project PROJECT] [--skip-api-enable] [--token-env-out
       TOKEN_ENV_OUT]

Interactively refresh local gcloud and Application Default Credentials. This
helper never writes credentials to the repository or to ``.env``. It uses
``gcloud`` for the interactive OAuth flows, verifies both token paths, and can
optionally write short-lived Document AI and Cloud Vision tokens to a user-
selected 0600 shell-env file outside the repository.

options:
  -h, --help            show this help message and exit
  --project PROJECT     Use this GCP project without prompting
  --skip-api-enable     Do not offer to enable required APIs
  --token-env-out TOKEN_ENV_OUT
                        Write the short-lived Document AI token to this 0600
                        file
```

## `exception_resolve.py`

```text
usage: exception_resolve.py [-h] --out OUT --report REPORT [--exceptions IN OUT] [--quiet] consensus
       authorization

Record an operator-authorized resolution for the findings holding a document
open. `consensus.py` is the only control that computes a document's
`review_status`, and it computes it once, from the readings. Every later
control -- the arithmetic vote, the corroboration applier, the classification
amendment -- copies that status forward untouched. A document that entered
review as `open_exception` therefore stays `open_exception` no matter what is
later established about it, and `canonical_export.py` admits only
`auto_accepted`, `sampled_verified` and `exception_resolved`.
`exception_resolved` is the status reserved for exactly this case, and nothing
in the repository wrote it. Three controls read it; none produced it. One run
made that concrete: the canonical export reported 0 of 716 documents after
every round of repair, including one where the arithmetic vote reported 620
documents promoted. The values changed and the status did not, so the export
excluded the whole corpus and named the consensus status as its reason. This
control writes that status, and only where an operator has authorized the
resolution of every finding still holding the document open. Four refusals
keep it from becoming an override channel: * an authorization that is not
`operator_authorized` is refused, so no client note and no model output can
clear a document by itself; * a document carrying an open finding the
authorization does not name is left exactly as it is and reported -- clearing
on a partial authorization would bury the findings nobody answered, which is
the failure this control exists to make impossible; * a document already clear
is never relabelled: `exception_resolved` claims something weaker than vendor
agreement and must not overwrite it; * a document the authorization names but
the consensus artifact does not carry is reported as an exception rather than
silently skipped. Nothing is deleted. A cleared document keeps
`prior_review_status` and gains a `resolution` block naming the authorization,
the moment, and every finding it answered, so a cleared status always reads
back to the signature that produced it. The input artifact is not modified;
this writes a new one. The report is not optional. A run where nothing cleared
and a run that was never executed must not look alike, so the artifact is
written either way and names every document left open and the findings that
held it. Clearing the status is only half the job. The final review gate is
built from the exception artifacts, and the canonical export refuses any
document the gate still holds an item against -- so an answered finding left
standing in those artifacts blocks the document the operator just cleared.
Pass `--exceptions IN OUT`, once per artifact, to mark each answered finding
`client_review_required: false`, which is the mechanism the gate already
honours and the one `classification_amend.py` established. Nothing is deleted:
the finding stays, carrying the authorization that answered it.

positional arguments:
  consensus            Retained consensus record artifact.
  authorization        Operator-authorized compiled plan JSON.

options:
  -h, --help           show this help message and exit
  --out OUT            new consensus artifact carrying the status
  --report REPORT      new report naming every authorized document this
                       control did not clear
  --exceptions IN OUT  Retained exception artifact and the new artifact to
                       write. Repeatable. Without it the findings this
                       authorization answered stay standing in the final
                       review gate, and the canonical export refuses the
                       document anyway.
  --quiet              suppress the summary
```

## `canonical_export.py`

```text
usage: canonical_export.py [-h] --manifest MANIFEST --consensus CONSENSUS [--arithmetic ARITHMETIC]
       [--final-review FINAL_REVIEW] --batch-id BATCH_ID [--registry-version REGISTRY_VERSION]
       [--classifications ARTIFACT] --out OUT --exceptions EXCEPTIONS [--quiet]

Build the canonical export envelope from retained run artifacts.

options:
  -h, --help            show this help message and exit
  --manifest MANIFEST   intake ingestion_manifest.json
  --consensus CONSENSUS
                        consensus.json
  --arithmetic ARITHMETIC
                        arithmetic.json; absent means unproved
  --final-review FINAL_REVIEW
                        final review queue JSON
  --batch-id BATCH_ID   load batch identifier
  --registry-version REGISTRY_VERSION
                        approved mapping registry version
  --classifications ARTIFACT
                        Repeatable classification_consensus_v1 artifact. A
                        document the extraction consensus left 'unknown' takes
                        the type classification accepted; a type extraction
                        established is kept.
  --out OUT             new canonical export JSON
  --exceptions EXCEPTIONS
                        retained exclusion JSON
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `canonical_deploy.py`

```text
usage: canonical_deploy.py [-h] [--schema SCHEMA] --out OUT [--database-url-env DATABASE_URL_ENV]
       [--execute]

Plan or explicitly deploy the canonical PostgreSQL schema.

options:
  -h, --help            show this help message and exit
  --schema SCHEMA       Canonical PostgreSQL DDL file the plan is built from.
  --out OUT             new non-secret deployment-plan JSON
  --database-url-env DATABASE_URL_ENV
                        Name of the environment variable holding the database
                        URL. The value is never written to the plan.
  --execute             apply with psql after writing the plan
```

## `canonical_load.py`

```text
usage: canonical_load.py [-h] --out OUT canonical_export

Create a vendor-neutral canonical CRM load orchestration plan.

positional arguments:
  canonical_export  Approved canonical export envelope.

options:
  -h, --help        show this help message and exit
  --out OUT         Destination path for this command's primary retained
                    artifact.
```

## `csv_api_staging.py`

```text
usage: csv_api_staging.py [-h] --out OUT [--review-session-id REVIEW_SESSION_ID] [--review-source-
       set-sha256 REVIEW_SOURCE_SET_SHA256] canonical_export canonical_load_plan

Create a checked, no-send CSV/API staging package.

positional arguments:
  canonical_export      Approved canonical export envelope.
  canonical_load_plan   Load plan the staging package must match exactly under
                        strict matching.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --review-session-id REVIEW_SESSION_ID
                        Optional visual intake session ID to link in the
                        staging manifest.
  --review-source-set-sha256 REVIEW_SOURCE_SET_SHA256
                        Optional visual intake source_set_sha256 paired with
                        --review-session-id.
```

## `crm_import_package.py`

```text
usage: crm_import_package.py [-h] --out OUT [--review-session-id REVIEW_SESSION_ID] [--review-
       source-set-sha256 REVIEW_SOURCE_SET_SHA256] canonical_export canonical_load_plan

Create verified no-send common CRM import files and a write plan.

positional arguments:
  canonical_export      Approved canonical export JSON to reshape.
  canonical_load_plan   Exact strict canonical load plan for the export.

options:
  -h, --help            show this help message and exit
  --out OUT             New output directory for the import package.
  --review-session-id REVIEW_SESSION_ID
                        Optional visual intake session ID to link in the
                        import manifest.
  --review-source-set-sha256 REVIEW_SOURCE_SET_SHA256
                        Optional visual intake source_set_sha256 paired with
                        --review-session-id.
```

## `retrieval_store.py`

```text
usage: retrieval_store.py [-h] {build,query,get} ...

Build or query a local canonical retrieval SQLite database.

positional arguments:
  {build,query,get}

options:
  -h, --help         show this help message and exit
```

## `retrieval_store.py build`

```text
usage: retrieval_store.py build [-h] --out OUT [--allow-empty] canonical_export

positional arguments:
  canonical_export  Approved canonical export envelope.

options:
  -h, --help        show this help message and exit
  --out OUT         Destination path for this command's primary retained
                    artifact.
  --allow-empty     retain a retrieval database with no approved chunks
```

## `retrieval_store.py query`

```text
usage: retrieval_store.py query [-h] [--limit LIMIT] database question

positional arguments:
  database       Path to the approved-fact retrieval SQLite database.
  question       Natural-language question converted to all-token full-text
                 search.

options:
  -h, --help     show this help message and exit
  --limit LIMIT  Maximum number of results to return.
```

## `retrieval_store.py get`

```text
usage: retrieval_store.py get [-h] database document_id

positional arguments:
  database     Path to the approved-fact retrieval SQLite database.
  document_id  Exact document identifier to retrieve. No fuzzy inference is
               applied.

options:
  -h, --help   show this help message and exit
```

## `retrieval_mcp.py`

```text
usage: retrieval_mcp.py [-h] [--ingestion-dir INGESTION_DIR] [--client-config CLIENT_CONFIG]
       [--ingestion-tenant INGESTION_TENANT] database

Serve a read-only canonical retrieval MCP endpoint over stdio.

positional arguments:
  database              Immutable approved-fact SQLite snapshot to serve.

options:
  -h, --help            show this help message and exit
  --ingestion-dir INGESTION_DIR
                        Opt in to a separate private visual-intake journal
                        directory; never the approved snapshot directory.
  --client-config CLIENT_CONFIG
                        Opt in to configured governed analytics and record-
                        change proposals using this client YAML.
  --ingestion-tenant INGESTION_TENANT
                        Tenant bound permanently to the optional local intake
                        journal (default: local).
```

## `retrieval_https.py`

```text
usage: retrieval_https.py [-h] --certificate CERTIFICATE --private-key PRIVATE_KEY [--bind-host
       BIND_HOST] [--port PORT] [--credential-env CREDENTIAL_ENV] [--enable] database

Serve approved retrieval facts through explicit TLS/bearer transport.

positional arguments:
  database              Path to the approved-fact retrieval SQLite database.

options:
  -h, --help            show this help message and exit
  --certificate CERTIFICATE
                        TLS certificate file served to clients.
  --private-key PRIVATE_KEY
                        TLS private key file. Keep it outside the repository.
  --bind-host BIND_HOST
                        Address to bind. Defaults to loopback; widen it only
                        with named client authorization.
  --port PORT           Port to listen on.
  --credential-env CREDENTIAL_ENV
                        Name of the environment variable holding the provider
                        credential. The variable name is retained; its value
                        is never copied into an artifact.
  --enable              Explicitly enable this disabled-by-default lane.
                        Without it the command refuses before performing any
                        external work.
```

## `retrieval_remote_mcp.py`

```text
usage: retrieval_remote_mcp.py [-h] [--ingestion-dir INGESTION_DIR] --certificate CERTIFICATE
       --private-key PRIVATE_KEY [--bind-host BIND_HOST] [--port PORT] --resource RESOURCE
       --authorization-server AUTHORIZATION_SERVER --introspection-endpoint INTROSPECTION_ENDPOINT
       --client-id CLIENT_ID [--client-secret-env CLIENT_SECRET_ENV] --tenant TENANT [--tenant-claim
       TENANT_CLAIM] [--roles-claim ROLES_CLAIM] --allow-role ALLOW_ROLE [--allow-origin
       ALLOW_ORIGIN] --audit-log AUDIT_LOG --export-dir EXPORT_DIR --download-base-url
       DOWNLOAD_BASE_URL [--export-ttl-seconds EXPORT_TTL_SECONDS] [--max-export-rows
       MAX_EXPORT_ROWS] [--canonical-export CANONICAL_EXPORT] [--canonical-load-plan
       CANONICAL_LOAD_PLAN] [--rate-limit RATE_LIMIT] [--max-request-bytes MAX_REQUEST_BYTES]
       [--enable] database

Serve approved CRM facts over OAuth-protected Streamable HTTP MCP.

positional arguments:
  database              Immutable approved-fact SQLite snapshot to serve.

options:
  -h, --help            show this help message and exit
  --ingestion-dir INGESTION_DIR
                        Opt in to visual intake MCP tools and
                        /api/ingestion/{operation}; private journal directory
                        permanently bound to --tenant. Never permits canonical
                        writes.
  --certificate CERTIFICATE
                        TLS certificate file.
  --private-key PRIVATE_KEY
                        TLS private key file outside the repository.
  --bind-host BIND_HOST
                        Listener address; widen only after deployment
                        approval.
  --port PORT           TLS listener port.
  --resource RESOURCE   Exact canonical public HTTPS resource URL ending in
                        /mcp.
  --authorization-server AUTHORIZATION_SERVER
                        OAuth authorization-server HTTPS issuer URL.
  --introspection-endpoint INTROSPECTION_ENDPOINT
                        RFC 7662 HTTPS token-introspection endpoint.
  --client-id CLIENT_ID
                        OAuth introspection client identifier; not a secret.
  --client-secret-env CLIENT_SECRET_ENV
                        Environment variable containing the introspection
                        client secret.
  --tenant TENANT       Exact tenant identifier this immutable snapshot
                        belongs to.
  --tenant-claim TENANT_CLAIM
                        Introspection claim carrying the tenant identifier.
  --roles-claim ROLES_CLAIM
                        Introspection claim carrying user roles.
  --allow-role ALLOW_ROLE
                        Authorized role; repeat to allow multiple roles.
  --allow-origin ALLOW_ORIGIN
                        Allowed HTTP Origin; repeat as needed. Requests
                        without Origin remain supported.
  --audit-log AUDIT_LOG
                        Append-only content-free audit-event file outside
                        version control.
  --export-dir EXPORT_DIR
                        Dedicated directory for immutable short-lived CRM
                        export jobs.
  --download-base-url DOWNLOAD_BASE_URL
                        Public HTTPS base URL used to construct signed export
                        download URLs.
  --export-ttl-seconds EXPORT_TTL_SECONDS
                        Signed export download lifetime from 60 through 3600
                        seconds.
  --max-export-rows MAX_EXPORT_ROWS
                        Maximum rows allowed in one CSV or XLSX export job.
  --canonical-export CANONICAL_EXPORT
                        Canonical export JSON used to build ZIP staging or
                        common-import packages.
  --canonical-load-plan CANONICAL_LOAD_PLAN
                        Canonical load plan JSON paired with --canonical-
                        export for package jobs.
  --rate-limit RATE_LIMIT
                        Maximum requests per client address per rolling
                        minute.
  --max-request-bytes MAX_REQUEST_BYTES
                        Maximum JSON-RPC or ingestion JSON API request bytes
                        (default: 1000000). Set at least 14000000 for a full
                        10 MB base64 page plus envelope; align proxy and
                        client limits.
  --enable              Explicitly enable the remote listener.
```

## `business_platform_deploy.py`

```text
usage: business_platform_deploy.py [-h] --out OUT config

Create a no-secret deployment and acceptance plan from client YAML.

positional arguments:
  config      Validated client-specific platform YAML.

options:
  -h, --help  show this help message and exit
  --out OUT   New deployment-plan JSON path; existing files are refused.
```

## `business_platform_server.py`

```text
usage: business_platform_server.py [-h] [--enable] config

Launch the OAuth MCP/API platform from one validated client YAML.

positional arguments:
  config      Client platform YAML path; credentials remain in named
              environment variables.

options:
  -h, --help  show this help message and exit
  --enable    Explicitly enable the network listener after deployment
              authorization.
```

## `business_record_changes.py`

```text
usage: business_record_changes.py [-h] --config CONFIG
       {schema,propose,preview,authorize,apply,reconcile} ...

Propose, preview, authorize, or apply governed business-record changes.

positional arguments:
  {schema,propose,preview,authorize,apply,reconcile}
    schema              Print the configured writable object schema.
    propose             Retain a proposal in the append-only change journal.
    preview             Preview a proposal against the immutable snapshot.
    authorize           Create a signed operator authorization for the current
                        immutable preview.
    apply               Apply an authorized preview through the configured
                        target adapter.
    reconcile           Read the target independently and compare every
                        expected field.

options:
  -h, --help            show this help message and exit
  --config CONFIG       Validated client-specific platform YAML file without
                        embedded secrets.
```

## `business_record_changes.py schema`

```text
usage: business_record_changes.py schema [-h]

options:
  -h, --help  show this help message and exit
```

## `business_record_changes.py propose`

```text
usage: business_record_changes.py propose [-h] --owner OWNER --request-key REQUEST_KEY --request
       REQUEST

options:
  -h, --help            show this help message and exit
  --owner OWNER         Proposal owner.
  --request-key REQUEST_KEY
                        Proposal request key.
  --request REQUEST     Proposal request.
```

## `business_record_changes.py preview`

```text
usage: business_record_changes.py preview [-h] --owner OWNER --change-id CHANGE_ID

options:
  -h, --help            show this help message and exit
  --owner OWNER         Exact proposal owner.
  --change-id CHANGE_ID
                        Retained proposal identifier.
```

## `business_record_changes.py authorize`

```text
usage: business_record_changes.py authorize [-h] --owner OWNER --change-id CHANGE_ID --approver
       APPROVER --expires-at EXPIRES_AT --request-key REQUEST_KEY --out OUT

options:
  -h, --help            show this help message and exit
  --owner OWNER         Authorization owner.
  --change-id CHANGE_ID
                        Authorization change id.
  --approver APPROVER   Authorization approver.
  --expires-at EXPIRES_AT
                        Authorization expires at.
  --request-key REQUEST_KEY
                        Authorization request key.
  --out OUT             Authorization out.
```

## `business_record_changes.py apply`

```text
usage: business_record_changes.py apply [-h] --owner OWNER --change-id CHANGE_ID --authorization
       AUTHORIZATION --request-key REQUEST_KEY --out OUT [--execute]

options:
  -h, --help            show this help message and exit
  --owner OWNER         Application owner.
  --change-id CHANGE_ID
                        Application change id.
  --authorization AUTHORIZATION
                        Application authorization.
  --request-key REQUEST_KEY
                        Application request key.
  --out OUT             Application out.
  --execute             Permit the configured live HTTP adapter; file adapters
                        remain no-send artifacts.
```

## `business_record_changes.py reconcile`

```text
usage: business_record_changes.py reconcile [-h] --owner OWNER --change-id CHANGE_ID --application
       APPLICATION --request-key REQUEST_KEY --out OUT

options:
  -h, --help            show this help message and exit
  --owner OWNER         Reconciliation owner.
  --change-id CHANGE_ID
                        Reconciliation change id.
  --application APPLICATION
                        Reconciliation application.
  --request-key REQUEST_KEY
                        Reconciliation request key.
  --out OUT             Reconciliation out.
```

## `retrieval_sidecar.py`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py critical`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py error`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py warning`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py info`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py debug`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `retrieval_sidecar.py trace`

```text
usage: retrieval_sidecar.py [-h] [--database DATABASE] [--host HOST] [--port PORT] [--log-level
       {critical,error,warning,info,debug,trace}]

Serve the internal retrieval sidecar over FastAPI and uvicorn.

options:
  -h, --help            show this help message and exit
  --database DATABASE   Immutable retrieval SQLite database. Overrides
                        RETRIEVAL_DB_URL and RETRIEVAL_ARTIFACT_ROOT.
  --host HOST           Bind address for the sidecar HTTP listener (default:
                        0.0.0.0).
  --port PORT           Port for the sidecar HTTP listener (default: 8080).
  --log-level {critical,error,warning,info,debug,trace}
                        Uvicorn log level (default: info).
```

## `run_retrieval_mcp.sh`

```text
Usage: bash scripts/run_retrieval_mcp.sh [RETRIEVAL_DATABASE]

Launch the local stdio MCP server for one immutable approved-fact SQLite snapshot.

Arguments:
  RETRIEVAL_DATABASE  Snapshot built by retrieval_store.py build. Defaults to
                      BUSINESS_DOCUMENT_RETRIEVAL_DB, then
                      ./business_retrieval.sqlite under the repository root.

Environment:
  BUSINESS_DOCUMENT_RETRIEVAL_DB  Default snapshot path when no argument is given.
  PYTHON_BIN                     Python executable; defaults to .venv/bin/python.

The launcher is local and read-only. Complete the Phase 6 activation gate in
references/mcp-production-integration.md before registering it with a client.
```

## `run_mcp_production_acceptance.sh`

```text
Usage: bash scripts/run_mcp_production_acceptance.sh [OUTPUT_DIRECTORY]

Exercise the complete local/remote approved-fact CRM MCP path with the tracked
fictional fixture and write hash-bound production-test evidence.

Arguments:
  OUTPUT_DIRECTORY  New directory for the load plan, staging, retrieval database,
                    exports, audit log, and mcp_production_acceptance_summary.json.
                    Defaults to output/mcp_production_acceptance in the repository.

Environment:
  PYTHON_BIN  Python executable; defaults to .venv/bin/python.

The command refuses an existing output directory. A pass establishes repository
code readiness only; deployed TLS, OAuth, workspace, operations, and the exact
approved client snapshot still require separate production acceptance.
```

## `release_check.py`

```text
usage: release_check.py [-h] [--root ROOT] [--tag TAG]

Validate the versioned repository release package.

options:
  -h, --help   show this help message and exit
  --root ROOT  Repository root to validate.
  --tag TAG    verify that a stable vMAJOR.MINOR.PATCH tag matches the
               checked-in release package
```

## `mutation_check.py`

```text
usage: mutation_check.py [-h] --max-surviving MAX_SURVIVING [--timeout-seconds TIMEOUT_SECONDS]
       [--modules [MODULES ...]] [--report REPORT]

Run mutation testing over the highest-stakes control modules. The repository's
100% branch gate is a strong floor and a known-incomplete one: `coverage.py`
derives branch arcs between statements, so both arms of a conditional
expression and every operand of a boolean short-circuit are invisible to it.
`scripts/` contains hundreds of each, concentrated in exactly the modules that
make decisions. Mutation testing measures what the gate cannot. The budget is
a ratchet, not a pass/fail line: lower `--max-surviving` as surviving mutants
are killed, and never raise it to make a run go green. **Not yet wired into
CI.** mutmut 3.x is TUI-oriented: in a non-interactive runner it stalls at
"Running stats" and emits no machine-readable summary. This runner therefore
refuses rather than reporting a count it did not receive -- an earlier version
defaulted to zero surviving mutants and passed CI green while mutmut had not
run at all, which is precisely the failure mode this repository exists to
prevent. Run it manually on a terminal, or replace the tool. Do not wire it
into CI until it produces a count non-interactively.

options:
  -h, --help            show this help message and exit
  --max-surviving MAX_SURVIVING
                        Maximum surviving mutants permitted. Required: the
                        budget is a deliberate ratchet, never inferred.
  --timeout-seconds TIMEOUT_SECONDS
                        Per-request provider timeout, in seconds.
  --modules [MODULES ...]
                        Control modules to mutate. Each must be configured in
                        [tool.mutmut].
  --report REPORT       Destination path for this command's report artifact.
```

## `branch_state.py`

```text
usage: branch_state.py [-h] [--main-branch MAIN_BRANCH] [--out OUT] [--quiet]

Determine whether a branch's work is already on the main branch. `main`
requires linear history, so it is merged by squash or rebase and a fully
merged branch is **not** an ancestor of it. `git branch --merged` therefore
reports merged work as unmerged and cannot be used to decide whether a branch
is safe to delete, which is how a large backlog of indistinguishable refs
accumulates. This resolves each branch by evidence instead: merged_by_ancestry
the tip is reachable from main merged_by_patch `git cherry` finds no unapplied
commits merged_by_subject the tip's subject appears in main's history
unresolved none of the above -- inspect it by hand, never delete it Reporting
only. It never deletes a branch; it tells an operator which ones the
documented procedure in BRANCHING.md permits deleting.

options:
  -h, --help            show this help message and exit
  --main-branch MAIN_BRANCH
                        Branch that merged work lands on. Each other branch is
                        classified against it.
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `client_delivery_package.py`

```text
usage: client_delivery_package.py [-h] --out-dir OUT_DIR --final-review FINAL_REVIEW [--canonical-
       export CANONICAL_EXPORT] [--evidence-graph EVIDENCE_GRAPH] [--review-file REVIEW_FILE]
       [--crm-file CRM_FILE] [--document DOCUMENT] [--allow-open-findings] [--source-root
       SOURCE_ROOT] [--quiet]

Assemble the final client-ready delivery folder and nothing more. A run
directory holds everything the pipeline needed: raw provider responses,
intermediate control artifacts, caches, resume state, and operator notes. None
of that belongs in a client hand-off, and several parts of it must never leave
the operator's machine at all. This copies only the deliverables into a new
no-clobber directory, refuses to copy anything on the exclusion list even when
a caller names it explicitly, and writes a manifest hashing every delivered
file. The evidence graph is delivered *cleaned*: nodes and edges whose
approval state is still a proposal are withheld, because a proposal is not a
fact and a client folder is read as a statement of fact. Withholding is not
hiding -- the exact count of every withheld state is retained in the manifest
and restated in the delivered README, so the client can see that proposals
existed and how many.

options:
  -h, --help            show this help message and exit
  --out-dir OUT_DIR     new client folder; must not exist
  --final-review FINAL_REVIEW
                        final review queue artifact
  --canonical-export CANONICAL_EXPORT
                        Approved canonical export to deliver.
  --evidence-graph EVIDENCE_GRAPH
                        Evidence graph to deliver. Proposal-state nodes and
                        edges are withheld and their counts retained.
  --review-file REVIEW_FILE
                        Reviewer-facing file to deliver. Repeatable.
  --crm-file CRM_FILE   Target-system staging file to deliver. Repeatable.
  --document DOCUMENT   Generated client document to deliver. Repeatable.
  --allow-open-findings
                        deliver an explicitly provisional package while
                        findings remain open
  --source-root SOURCE_ROOT
                        run directory the deliverables come from; defaults to
                        the final review's directory
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `run_sentinel.py`

```text
usage: run_sentinel.py [-h] {preflight,watch} ...

Catch a broken lane while a run is happening, not after it finishes. A full
trial spent a corpus before anyone noticed that Google Document AI had
returned HTTP 404 on all 18 pages. Every page was retained as a failure, the
adapter exited zero, and the run continued believing it had a third
independent vendor. The evidence was on disk the whole time -- every raw
response said ``document_ai_http_404`` -- and nothing read it until the run
was over. Two checks, and neither costs a token: ``preflight`` runs before the
run. It resolves every enabled lane's provider and credential variable and
reports the ones that are missing. With ``--probe`` it makes one minimal
authenticated request per distinct provider endpoint, which is what separates
"the variable is set" from "the endpoint accepts it". A wrong processor id is
then a five-second refusal instead of a corpus of failures. ``watch`` runs
during the run. It polls the run directory, reads only retained files, and
classifies every retained provider failure it finds. A lane whose pages are
all failing is reported the moment the evidence exists, and ``--fail-fast``
exits non-zero so a supervising script can stop the run. Both are
deterministic and read-only with respect to the run: no artifact is written
except the optional report, and no lane is retried from here. A credential or
configuration failure is the operator's to fix, and the report names which of
the two it is, because those have different remedies and only one of them is
worth retrying.

positional arguments:
  {preflight,watch}
    preflight        verify every enabled lane has a credential
    watch            report retained provider failures during a run

options:
  -h, --help         show this help message and exit
```

## `run_sentinel.py preflight`

```text
usage: run_sentinel.py preflight [-h] [--out OUT] [--quiet]

options:
  -h, --help  show this help message and exit
  --out OUT   New JSON report path.
  --quiet     Suppress the console summary only. Retained artifacts, findings,
              and exit status are unchanged.
```

## `run_sentinel.py watch`

```text
usage: run_sentinel.py watch [-h] [--interval INTERVAL] [--once] [--fail-fast] [--out OUT] [--quiet]
       run_dir

positional arguments:
  run_dir              Run directory whose retained failures are read.

options:
  -h, --help           show this help message and exit
  --interval INTERVAL  Seconds between polls.
  --once               Take one snapshot and exit.
  --fail-fast          Exit non-zero as soon as a lane is failing for a
                       credential or configuration reason.
  --out OUT            New JSON report path; used with --once.
  --quiet              Suppress the console summary only. Retained artifacts,
                       findings, and exit status are unchanged.
```

## `pipeline_plan.py`

```text
usage: pipeline_plan.py [-h] [--objective {balanced,fewest-client-questions,least-spend}] [--key-
       field FIELD] [--out OUT] [--quiet] run_dir

Propose the pipeline this corpus needs, from the run's own retained artifacts.

positional arguments:
  run_dir               Run directory to measure. Read-only; nothing is
                        written into it.

options:
  -h, --help            show this help message and exit
  --objective {balanced,fewest-client-questions,least-spend}
                        Which way a close call falls. Never changes whether a
                        measurement is believed.
  --key-field FIELD     Header field carrying the attribution key, repeatable.
                        Defaults to attribution.py's own list.
  --out OUT             Destination path for the JSON plan artifact.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `run_lane_coverage.py`

```text
usage: run_lane_coverage.py [-h] [--require LANE] [--generation GENERATION] [--out OUT] [--quiet]
       run_dir

Report which pipeline lanes a run actually executed. Rule 9 says a control
that processed nothing has not passed. Nothing enforced the same rule one
level up: a lane nobody invoked leaves no artifact, no exception, and no
trace, so a run reaches delivery looking complete while whole controls were
never run at all. That is not hypothetical. A full trial reached CRM staging
with the source-table lane, both mapping lanes, template drift, every
relationship lane, sampling, and analytics never invoked -- fourteen lane
families -- and no command anywhere said so, because each was individually
optional and none of them ran to report it. This command scans a run directory
for the artifacts each lane leaves behind and names the lanes that left none.
It counts everything under that directory, so a run directory must hold only
that run's evidence: synthetic inputs, smoke tests, and scratch outputs left
inside it are counted as lanes that ran, and the report then overstates the
run. Keep them somewhere else. It reads only retained files, contacts no
provider, and changes nothing. It is a completeness report, not a gate on the
evidence: a lane may be deliberately skipped, and saying so out loud is the
point. ``--require`` turns a named lane into a failure when it produced
nothing, so a run profile that must include a lane can be enforced. The
catalogue below is checked against the live scripts by
``agent_surface_check.py``, so a lane added to the repository and forgotten
here fails the release gate rather than silently going unmeasured.

positional arguments:
  run_dir               Run directory whose retained lanes are counted.

options:
  -h, --help            show this help message and exit
  --require LANE        Fail when this lane produced nothing. Repeatable. Lane
                        names are the ones this command prints.
  --generation GENERATION
                        Generation manifest declaring the authoritative
                        artifact per lane. A restarted run holds several
                        attempts, and without this any of them credits its
                        lane -- so a discarded attempt can make a lane read as
                        covered. Where a lane is declared, only its
                        authoritative artifacts count as evidence.
  --out OUT             New JSON report path.
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```

## `run_generation.py`

```text
usage: run_generation.py [-h] {template,verify} ...

Declare which retained artifact is the authoritative one for each lane. A run
that was restarted holds more than one artifact per lane. Extraction may have
been attempted five times, templates rebuilt twice, tables begun six times.
Every attempt is retained on purpose -- a stopped lane is evidence, and
deleting it would hide what happened -- but only one of them is the reading
the run stands behind. Nothing in the run says which. `run_lane_coverage.py`
scans the directory and credits a lane from any artifact that matches, so a
superseded attempt can make a lane look covered when the artifact the operator
actually relies on was never written. That is a false positive in the one
report whose job is to say what did not run, and it is worse than the false
negatives it was built to catch. This command records the decision as an
artifact. It never chooses: an operator declares the authoritative path per
lane and the reason each other attempt was superseded, and this verifies the
declaration against what the run contains. `template` scaffolds a declaration
from the run, listing every artifact that currently credits each lane so none
is silently forgotten. `verify` reports three failures a hand-written
declaration is prone to: * a declared artifact that does not exist, so the
lane it claims has nothing behind it; * an undeclared artifact that would
credit a lane on a directory scan, which is exactly the false positive this
exists to prevent; * a lane declared authoritative and superseded for the same
path, which is a contradiction rather than a preference. It changes nothing
and clears no control. A declaration is an operator statement about which
reading the run stands behind, not an approval of that reading.

positional arguments:
  {template,verify}
    template         scaffold a declaration from the run
    verify           verify a declaration against the run

options:
  -h, --help         show this help message and exit
```

## `run_generation.py template`

```text
usage: run_generation.py template [-h] --out OUT [--quiet] run_dir

positional arguments:
  run_dir     Run directory to scaffold from.

options:
  -h, --help  show this help message and exit
  --out OUT   New declaration path.
  --quiet     Suppress the console summary only. Retained artifacts, findings,
              and exit status are unchanged.
```

## `run_generation.py verify`

```text
usage: run_generation.py verify [-h] --manifest MANIFEST [--out OUT] [--quiet] run_dir

positional arguments:
  run_dir              Run directory the declaration describes.

options:
  -h, --help           show this help message and exit
  --manifest MANIFEST  Declaration to verify.
  --out OUT            New JSON report path.
  --quiet              Suppress the console summary only. Retained artifacts,
                       findings, and exit status are unchanged.
```

## `agent_surface_check.py`

```text
usage: agent_surface_check.py [-h] [--out OUT] [--write-cli-help-catalogue] [--quiet]

Keep the agent-facing surfaces in step with the code they describe. Operating
instructions rot silently: a script is renamed, and the skill folder still
tells an agent to run the old name. Nothing fails, because nobody runs the
instructions as a test. This makes them one. Every `scripts/NAME.py` named in
the agent surfaces must exist and be indexed in the command-line reference,
and every documented root entry point must be reachable. Reporting only; it
changes nothing.

options:
  -h, --help            show this help message and exit
  --out OUT             Destination path for this command's primary retained
                        artifact.
  --write-cli-help-catalogue
                        regenerate the checked-in complete CLI help catalogue
                        from live parser help
  --quiet               Suppress the console summary only. Retained artifacts,
                        findings, and exit status are unchanged.
```
